---
phase: 05-core-oxc-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml  # workspace root
  - crates/qwik-optimizer-oxc/Cargo.toml
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/src/types.rs
  - crates/qwik-optimizer-oxc/src/words.rs
  - crates/qwik-optimizer-oxc/src/hash.rs
  - crates/qwik-optimizer-oxc/src/errors.rs
  - crates/qwik-optimizer-oxc/src/is_const.rs
autonomous: true
requirements: [IMPL-08]

must_haves:
  truths:
    - "Crate compiles with `cargo build -p qwik-optimizer-oxc`"
    - "All public types match spec TransformModulesOptions, TransformOutput, TransformModule, SegmentAnalysis, SegmentKind definitions"
    - "Hash generation produces deterministic output using SipHash 1-3 with seed (0,0)"
    - "dollar_to_qrl_name correctly maps $ functions to Qrl counterparts"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/Cargo.toml"
      provides: "Crate manifest with all dependencies per D-36 (OXC 0.123)"
      contains: "oxc"
    - path: "crates/qwik-optimizer-oxc/src/types.rs"
      provides: "All public and internal types"
      min_lines: 150
    - path: "crates/qwik-optimizer-oxc/src/words.rs"
      provides: "String constants, dollar_to_qrl_name, classify_ctx_kind"
      min_lines: 50
    - path: "crates/qwik-optimizer-oxc/src/hash.rs"
      provides: "SipHash + base64 segment name generation"
      min_lines: 40
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/lib.rs"
      to: "crates/qwik-optimizer-oxc/src/types.rs"
      via: "mod types; pub use types::*"
      pattern: "pub use types"
---

<objective>
Create the qwik-optimizer-oxc crate with all foundation modules (Layer 1): types, string constants, hash generation, error helpers, and constness analysis. This establishes the data structures and utilities that every subsequent plan depends on.

Purpose: Per D-33, the crate lives at `crates/qwik-optimizer-oxc/` in the workspace. Per D-36, OXC 0.123.0 pinned. Per D-08, all patterns must be idiomatic OXC. This plan creates the foundation with zero transform logic -- pure data definitions and utility functions.

Output: A compiling Rust crate with all types, constants, and utility modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-oxc-implementation/05-CONTEXT.md
@.planning/phases/05-core-oxc-implementation/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crate scaffold with Cargo.toml and all dependencies</name>
  <files>Cargo.toml, crates/qwik-optimizer-oxc/Cargo.toml, crates/qwik-optimizer-oxc/src/lib.rs</files>
  <read_first>
    - /Users/scottweaver/Projects/qwik-optimizer-next/Cargo.toml (workspace root -- needs members array added)
    - /Users/scottweaver/Projects/qwik-optimizer-next/specification/.planning/phases/05-core-oxc-implementation/05-RESEARCH.md (lines 83-124 for exact dependency versions and feature flags)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/Cargo.toml (Jack's manifest for reference)
  </read_first>
  <action>
    1. Update workspace root `Cargo.toml` to add `[workspace]` section with `members = ["crates/qwik-optimizer-oxc"]` and set `resolver = "3"`. Keep the existing `[package]` for the root crate but add the workspace config. Per D-33.

    2. Create `crates/qwik-optimizer-oxc/Cargo.toml` with these exact dependencies per D-36 and research:
       ```toml
       [package]
       name = "qwik-optimizer-oxc"
       version = "0.1.0"
       edition = "2024"

       [dependencies]
       oxc = { version = "=0.123.0", features = ["codegen", "semantic", "serialize", "ast_visit"] }
       oxc_traverse = "=0.123.0"
       serde = { version = "1", features = ["derive"] }
       serde_json = "1"
       anyhow = "1"
       base64 = "0.22"
       siphasher = "1"
       indexmap = { version = "2", features = ["serde"] }
       pathdiff = "0.2"
       path-slash = "0.2"
       rayon = { version = "1", optional = true }

       [dev-dependencies]
       insta = { version = "1", features = ["json"] }

       [features]
       default = []
       parallel = ["rayon"]
       ```

    3. Create `crates/qwik-optimizer-oxc/src/lib.rs` that declares all planned modules:
       ```rust
       pub mod types;
       pub mod words;
       pub mod hash;
       pub mod errors;
       pub mod is_const;
       // Future modules (uncomment as implemented):
       // pub mod parse;
       // pub mod collector;
       // ... etc
       ```
       Also add `pub use types::*;` for the public API types.

    4. Verify: `cargo build -p qwik-optimizer-oxc` compiles (will need stub modules).
  </action>
  <verify>
    <automated>cd /Users/scottweaver/Projects/qwik-optimizer-next && cargo check -p qwik-optimizer-oxc 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Workspace root Cargo.toml has `[workspace]` with `members` including the new crate
    - Crate Cargo.toml has all dependencies at exact versions from research
    - OXC features include codegen, semantic, serialize, ast_visit (NOT transformer, minifier, etc.)
    - `cargo check` succeeds
  </acceptance_criteria>
  <done>Crate compiles with all dependencies resolved. No SWC crates present (per D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement foundation modules (types, words, hash, errors, is_const)</name>
  <files>
    crates/qwik-optimizer-oxc/src/types.rs,
    crates/qwik-optimizer-oxc/src/words.rs,
    crates/qwik-optimizer-oxc/src/hash.rs,
    crates/qwik-optimizer-oxc/src/errors.rs,
    crates/qwik-optimizer-oxc/src/is_const.rs
  </files>
  <read_first>
    - /Users/scottweaver/Projects/qwik-optimizer-next/specification/qwik-optimizer-spec.md (search for "TransformModulesOptions", "TransformOutput", "TransformModule", "SegmentAnalysis", "SegmentKind", "MinifyMode", "EmitMode", "EntryStrategy" type definitions)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/src/types.rs (Jack's type definitions for OXC API reference only)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/src/words.rs (dollar_to_qrl_name mapping reference)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/src/hash.rs (SipHash + base64 reference)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/src/is_const.rs (constness analysis reference)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/src/errors.rs (error helpers reference)
  </read_first>
  <behavior>
    - Test: dollar_to_qrl_name("component$") == "componentQrl"
    - Test: dollar_to_qrl_name("$") == "qrl"
    - Test: dollar_to_qrl_name("useTask$") == "useTaskQrl"
    - Test: hash generation for known input produces deterministic base64 output matching SWC
    - Test: classify_ctx_kind correctly categorizes function types
    - Test: SegmentKind serialization matches expected JSON values
    - Test: TransformOutput::default() produces empty valid output
  </behavior>
  <action>
    **types.rs** -- Define ALL public and internal types from the spec. Key types:
    - `TransformModulesOptions` with all fields from spec Section 7.1 (src_dir, root_dir, input, source_maps, minify, transpile_ts, transpile_jsx, preserve_filenames, explicit_extensions, entry_strategy, mode, scope, core_module, strip_exports, strip_ctx_name, strip_event_handlers, reg_ctx_name, is_server)
    - `TransformModuleInput` (path, code, dev_path)
    - `TransformOutput` (modules: Vec<TransformModule>, diagnostics: Vec<Diagnostic>)
    - `TransformModule` (path, code, map, segment, is_entry)
    - `SegmentAnalysis` (origin, name, entry, display_name, hash, canonical_filename, path, extension, parent, ctx_kind, ctx_name, captures, loc, param_names)
    - `SegmentKind` enum (Function, Event, JSXProp)
    - `MinifyMode` enum (Simplify, None)
    - `EmitMode` enum (Prod, Dev, Lib, Test, Hmr)
    - `EntryStrategy` enum (Inline, Hoist, Single, Hook, Segment, Component, Smart)
    - `Diagnostic` type
    - All enums need Serialize/Deserialize, Debug, Clone. Use serde rename attributes where JSON representation differs from Rust naming.
    - Implement `TransformOutput::append(&mut self, other: &mut TransformOutput)` for merging results.
    - Implement `TransformOutput::default()`.

    **words.rs** -- String constants and helper functions:
    - `QWIK_CORE_MODULE`: `"@qwik.dev/core"` and legacy `"@builder.io/qwik"`
    - `dollar_to_qrl_name(name: &str) -> String`: Maps `foo$` to `fooQrl`, `$` to `qrl`
    - `classify_ctx_kind(callee_name: &str) -> &'static str`: Returns "function", "event", "jsxProp" etc.
    - List of known `$`-suffixed marker functions from spec Section 2.1

    **hash.rs** -- Deterministic segment hash:
    - Use `siphasher::sip::SipHasher13` with key `(0, 0)` (matching SWC behavior)
    - Hash input = canonical segment content
    - Encode with base64 URL-safe no-pad, truncate to 11 chars
    - Function: `generate_hash(display_name: &str, code: &str) -> String`

    **errors.rs** -- Diagnostic creation helpers:
    - `create_diagnostic(message: &str, severity: DiagnosticSeverity, span: Option<(u32, u32)>) -> Diagnostic`

    **is_const.rs** -- Expression constness analysis for signal optimization:
    - `is_const_expression(expr: &Expression) -> bool`: Returns true for literals, template literals without expressions, identifiers pointing to known constants
    - Used by signal optimization (CONV-07) to determine if expressions can be statically analyzed
  </action>
  <verify>
    <automated>cd /Users/scottweaver/Projects/qwik-optimizer-next && cargo test -p qwik-optimizer-oxc -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All public types match spec definitions (field names, types, defaults)
    - Serde serialization of types produces JSON matching fixtures.json format
    - dollar_to_qrl_name handles all known $ functions
    - Hash generation is deterministic (same input = same output across runs)
    - `cargo build -p qwik-optimizer-oxc` compiles cleanly
    - Unit tests for words.rs and hash.rs pass
  </acceptance_criteria>
  <done>All Layer 1 foundation modules compile and pass unit tests. Types are serde-compatible with the fixture/snapshot format.</done>
</task>

</tasks>

<verification>
- `cargo build -p qwik-optimizer-oxc` succeeds with no warnings
- `cargo test -p qwik-optimizer-oxc` passes all unit tests
- `cargo clippy -p qwik-optimizer-oxc` is clean
- No SWC crate dependencies in Cargo.lock
</verification>

<success_criteria>
The qwik-optimizer-oxc crate exists at `crates/qwik-optimizer-oxc/`, compiles with OXC 0.123 and all dependencies, has complete type definitions matching the spec, and passes unit tests for hash generation and dollar-to-qrl name mapping.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-oxc-implementation/05-01-SUMMARY.md`
</output>
