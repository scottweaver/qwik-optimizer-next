---
phase: 04-public-api-bindings-cross-cutting-specification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specification/qwik-optimizer-spec.md
autonomous: true
requirements:
  - SPEC-18
  - SPEC-19
  - SPEC-20

must_haves:
  truths:
    - "A reader can find the complete TransformModulesOptions struct with all 16 fields, their types, defaults, and JSON wire names"
    - "A reader can find TransformOutput, TransformModule, SegmentAnalysis, SegmentKind type definitions with field semantics"
    - "A reader can find the Diagnostic type with all 7 fields, 3 DiagnosticCategory variants, 3 error codes (C02, C03, C05), and SourceLocation"
    - "All types are documented as Rust struct definitions with doc comments per D-31"
  artifacts:
    - path: "specification/qwik-optimizer-spec.md"
      provides: "Public API Types section with all input, output, enum, and diagnostic type definitions"
      contains: "## Public API Types"
  key_links:
    - from: "## Public API Types"
      to: "TransformModulesOptions"
      via: "subsection"
      pattern: "### TransformModulesOptions"
    - from: "## Public API Types"
      to: "Diagnostic"
      via: "subsection"
      pattern: "### Diagnostic"
---

<objective>
Write the "## Public API Types" section of the Qwik Optimizer spec, documenting all input types (TransformModulesOptions, TransformModuleInput), output types (TransformOutput, TransformModule, SegmentAnalysis), enums (EntryStrategy, SegmentKind, EmitMode, MinifyMode), and diagnostics (Diagnostic, DiagnosticCategory, DiagnosticScope, SourceLocation, error codes).

Purpose: These type definitions are the foundational reference for the entire public API -- binding contracts and implementation both depend on them. Per D-31, they are documented as actual Rust struct definitions with doc comments.

Output: New "## Public API Types" section appended to qwik-optimizer-spec.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-api-bindings-cross-cutting-specification/04-CONTEXT.md
@.planning/phases/04-public-api-bindings-cross-cutting-specification/04-RESEARCH.md

Source files to read for type extraction:
- /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/lib.rs (TransformModulesOptions, TransformModuleInput)
- /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/parse.rs (TransformOutput, TransformModule, SegmentAnalysis, MinifyMode, EmitMode)
- /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/transform.rs (SegmentKind)
- /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/entry_strategy.rs (EntryStrategy, EntryPolicy)
- /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/utils.rs (Diagnostic, DiagnosticCategory, DiagnosticScope, SourceLocation)
- /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/errors.rs (Error codes C02, C03, C05)
- /Users/scottweaver/Projects/qwik/packages/optimizer/src/types.ts (TypeScript contract for wire format verification)
- /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/src/types.rs (Jack's OXC types for cross-reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write input types and enums subsections</name>
  <files>specification/qwik-optimizer-spec.md</files>
  <read_first>
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/lib.rs
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/entry_strategy.rs
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/parse.rs (for MinifyMode, EmitMode)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/src/types.ts (for wire format of EntryStrategy)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/src/types.rs (for Jack's defaults and doc comments)
    - specification/qwik-optimizer-spec.md (last 30 lines to find insertion point)
  </read_first>
  <action>
Append a new "## Public API Types" section to the end of qwik-optimizer-spec.md. This task writes the first half: input types and enums.

Write these subsections in order:

### TransformModulesOptions
- Rust struct definition with `#[derive(Debug, Serialize, Deserialize)]` and `#[serde(rename_all = "camelCase")]`
- All 16 fields using `String` (not SWC Atom) per D-31 and Research Pitfall 1
- Doc comment on each field explaining: purpose, type, default value, valid values
- Note that SWC does not implement Default (all fields required in struct), but Jack adds `#[serde(default)]` for ergonomic JSON deserialization -- spec should document sensible defaults
- Note `is_server` defaults to `true` when `None`
- Note `core_module` defaults to `@qwik.dev/core` internally when `None`

### TransformModuleInput
- 3-field struct: path, dev_path, code

### EntryStrategy
- Use `#[serde(tag = "type")]` tagged object form per Research Pitfall 2 (matches TypeScript types.ts contract, not SWC's bare string form)
- Document all 7 variants with JSON examples: `{ "type": "inline" }` etc.
- Note Hook is deprecated alias for Segment

### EmitMode
- 5 variants: Prod, Dev, Lib, Test, Hmr
- JSON serialization: `"prod"`, `"dev"`, `"lib"`, `"test"`, `"hmr"`

### MinifyMode
- 2 variants: Simplify, None
- JSON serialization: `"simplify"`, `"none"`

### SegmentKind
- SWC has 2 variants (Function, EventHandler) per D-05 source of truth
- Note Jack's JSXProp addition as an OXC extension per Research Pitfall 3
- JSON: `"function"`, `"eventHandler"`

Format: Each subsection has an opening description paragraph, then the Rust struct/enum definition in a code block with doc comments on each field, then a "JSON Wire Format" sub-subsection showing how it serializes/deserializes with a JSON example. Cross-reference existing spec sections where types are used (e.g., EntryStrategy references "Entry Strategies" section from Phase 3).
  </action>
  <verify>
    <automated>grep -c "### TransformModulesOptions\|### EntryStrategy\|### EmitMode\|### MinifyMode\|### SegmentKind\|### TransformModuleInput" specification/qwik-optimizer-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - "## Public API Types" heading exists at end of spec
    - TransformModulesOptions struct has exactly 16 + 2 fields (16 original + reg_ctx_name + is_server = 18 total; verify against SWC source)
    - EntryStrategy uses tagged object serialization format
    - SegmentKind documents 2 SWC variants with note about Jack's JSXProp
  </acceptance_criteria>
  <done>6 subsections (TransformModulesOptions, TransformModuleInput, EntryStrategy, EmitMode, MinifyMode, SegmentKind) written with Rust definitions and JSON wire format examples</done>
</task>

<task type="auto">
  <name>Task 2: Write output types and diagnostics subsections</name>
  <files>specification/qwik-optimizer-spec.md</files>
  <read_first>
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/parse.rs (TransformOutput, TransformModule, SegmentAnalysis)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/utils.rs (Diagnostic, DiagnosticCategory, DiagnosticScope, SourceLocation)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/errors.rs (error codes C02, C03, C05)
    - specification/qwik-optimizer-spec.md (last 30 lines to find insertion point after Task 1's content)
  </read_first>
  <action>
Continue the "## Public API Types" section by appending output type and diagnostic subsections.

Write these subsections:

### TransformOutput
- 4-field struct: modules, diagnostics, is_type_script, is_jsx
- Note this is the top-level return type from transform_modules()

### TransformModule
- 6 fields, but `order` has `#[serde(skip_serializing)]` per Research Pitfall 4 -- document it as internal-only
- `segment` is `Option<SegmentAnalysis>` -- present only for extracted segments, None for root module
- `is_entry` marks entry point modules

### SegmentAnalysis
- All 15 fields with doc comments
- `ctx_kind` uses SegmentKind enum (cross-ref)
- `captures` is bool (whether segment captures outer scope vars)
- `loc` serializes as `[start, end]` tuple
- `param_names` and `capture_names` use `#[serde(skip_serializing_if = "Option::is_none")]`

### Diagnostic
- 7 fields with doc comments
- `scope` is always `DiagnosticScope::Optimizer`

### DiagnosticCategory
- 3 variants: Error, Warning, SourceError
- Explain SourceError semantics: error for project source, warning for node_modules

### SourceLocation
- 6 fields with the column indexing gotcha per Research Pitfall 5
- Document: start_col is 1-indexed, end_col is 0-indexed display column
- Include the SWC source comment explaining why

### Error Codes
- Table of 3 codes: C02 (FunctionReference), C03 (CanNotCapture), C05 (MissingQrlImplementation)
- Note codes are sparse (no C01, C04) per Research Pitfall 6
- Include trigger condition and message pattern for each

Format: Same as Task 1 -- Rust definitions with doc comments, JSON wire format examples. For Diagnostic, include a complete JSON example showing a C02 error with highlights and suggestions.
  </action>
  <verify>
    <automated>grep -c "### TransformOutput\|### TransformModule\|### SegmentAnalysis\|### Diagnostic\|### DiagnosticCategory\|### SourceLocation\|### Error Codes" specification/qwik-optimizer-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - TransformModule.order documented with skip_serializing annotation
    - SegmentAnalysis has all 15 fields
    - Diagnostic section includes all 3 error codes with message patterns
    - SourceLocation documents column indexing asymmetry
    - Complete JSON example of a Diagnostic included
  </acceptance_criteria>
  <done>7 subsections (TransformOutput, TransformModule, SegmentAnalysis, Diagnostic, DiagnosticCategory, SourceLocation, Error Codes) written with complete type definitions and wire format documentation</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "^## Public API Types" specification/qwik-optimizer-spec.md` returns 1
2. `grep -c "^### " specification/qwik-optimizer-spec.md` shows increase of ~13 subsections
3. All type names from SPEC-18, SPEC-19, SPEC-20 are present: TransformModulesOptions, TransformOutput, TransformModule, SegmentAnalysis, SegmentKind, Diagnostic, DiagnosticCategory, SourceLocation
</verification>

<success_criteria>
- SPEC-18 satisfied: TransformModulesOptions documented with all config fields, types, defaults, valid values
- SPEC-19 satisfied: TransformOutput, TransformModule, SegmentAnalysis, SegmentKind fully documented
- SPEC-20 satisfied: Diagnostic type documented with all fields, 3 category variants, 3 error codes
- All types use Rust struct definitions with doc comments per D-31
- JSON wire format examples for every type per D-31
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-api-bindings-cross-cutting-specification/04-01-SUMMARY.md`
</output>
