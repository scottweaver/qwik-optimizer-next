---
phase: 06-strategies-modes-binding-implementation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/src/code_move.rs
autonomous: true
requirements: [IMPL-04]

must_haves:
  truths:
    - "HMR mode injects _useHmr(devPath) as first statement in component$ bodies only"
    - "HMR mode does NOT inject _useHmr in useTask$, $(), or other non-component dollar calls"
    - "Lib mode skips segment extraction — all QRLs stay as inlinedQrl() in root module"
    - "Test mode skips const replacement but runs all other transforms"
    - "Dev mode uses qrlDEV/inlinedQrlDEV with source location objects"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "HMR _useHmr() injection logic"
      contains: "_useHmr"
    - path: "crates/qwik-optimizer-oxc/src/lib.rs"
      provides: "Lib mode segment-extraction skip verification"
      contains: "EmitMode::Lib"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/transform.rs"
      to: "crates/qwik-optimizer-oxc/src/types.rs"
      via: "EmitMode enum matching for mode-specific behavior"
      pattern: "EmitMode::Hmr|EmitMode::Lib|EmitMode::Test"
---

<objective>
Wire remaining emit mode gaps: HMR _useHmr() injection in component$ bodies and validate Lib/Test/Dev mode behavioral variations.

Purpose: The core crate already handles Dev/Prod QRL variant switching and most mode branching from Phase 5. The gaps are: HMR _useHmr() injection (only in component$ bodies), Lib mode segment-extraction skip behavior (may already work via is_inline_mode), and Test mode const-replacement skip (already implemented in const_replace.rs but needs validation). This plan closes those gaps.

Output: All 5 emit modes producing correct behavioral variations, validated with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-strategies-modes-binding-implementation/06-CONTEXT.md
@.planning/phases/06-strategies-modes-binding-implementation/06-RESEARCH.md
@.planning/phases/06-strategies-modes-binding-implementation/06-01-SUMMARY.md

@crates/qwik-optimizer-oxc/src/transform.rs
@crates/qwik-optimizer-oxc/src/lib.rs
@crates/qwik-optimizer-oxc/src/const_replace.rs

<interfaces>
From transform.rs (after Plan 01):
```rust
pub(crate) struct QwikTransform {
    pub(crate) mode: EmitMode,
    pub(crate) entry_strategy: EntryStrategy,
    pub(crate) segment_stack: Vec<SegmentScope>,
    pub(crate) needs_noop_qrl_import: bool,
    // is_qcomponent is tracked per SegmentScope or via marker detection
}
```

From types.rs:
```rust
pub enum EmitMode { Lib, Prod, Dev, Hmr, Test }
```

From const_replace.rs:
```rust
// const_replace already skips for EmitMode::Test and EmitMode::Lib
pub(crate) fn replace_build_constants(program, config, collect, allocator);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement HMR _useHmr() injection and validate emit mode gaps</name>
  <files>crates/qwik-optimizer-oxc/src/transform.rs, crates/qwik-optimizer-oxc/src/lib.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/src/transform.rs (search for "is_qcomponent" or "component$" detection logic, and the segment creation code for component$ markers around lines 1100-1300)
    - crates/qwik-optimizer-oxc/src/const_replace.rs (first 30 lines for mode skip logic)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/transform.rs (lines 4117-4144 for SWC HMR injection reference)
    - /Users/scottweaver/Projects/qwik-optimizer-next/specification/qwik-optimizer-spec.md (lines 5036-5099 for HMR spec with example)
  </read_first>
  <behavior>
    - Test 1: HMR mode with component$ produces segment containing _useHmr(devPath) as first statement
    - Test 2: HMR mode with useTask$ does NOT inject _useHmr
    - Test 3: HMR mode with $() (bare dollar) does NOT inject _useHmr
    - Test 4: Lib mode produces inlinedQrl() in root module with no separate segment files
    - Test 5: Test mode preserves isServer/isBrowser as identifiers (not replaced with booleans)
  </behavior>
  <action>
    **Part A: HMR _useHmr() injection (per D-41)**

    In transform.rs, find where component$ segment bodies are created. When `self.mode == EmitMode::Hmr` AND the marker is `component$` (detected by checking if the callee name in marker_functions resolves to "component$"):

    1. Before creating the segment body code (the span-based extraction), prepend `_useHmr("{dev_path}");\n` to the segment body string. The dev_path comes from `self.rel_path` or the `dev_path` parameter passed through to transform_code.
    2. Track `needs_use_hmr_import: bool` on QwikTransform. When HMR injection occurs, set it to true.
    3. In the segment module construction (code_move.rs new_module), when `needs_use_hmr_import` is relevant, add `_useHmr` to the segment's imports from core module. Actually — the simpler approach: prepend the `_useHmr` call to the extracted segment body string before it gets assembled. The import will be auto-resolved by the segment's import resolution logic in code_move.rs if `_useHmr` is detected as a used identifier.

    The key detail: `_useHmr` injection happens in the SEGMENT body (the extracted function), not in the root module. Look at the spec example (lines 5078-5079): the segment export function has `_useHmr("/user/qwik/src/test.tsx");` as its first statement.

    The `is_qcomponent` check: In the existing code, when processing a marker function call, the transform knows the callee name. Check if it equals "component$" (the original specifier, not the Qrl variant). Only component$ triggers HMR, not componentQrl or other markers.

    **Part B: Validate Lib mode segment-extraction skip**

    The existing code already has `is_inline_mode()` returning true for Lib mode (since Inline and Hoist share the inline path, and Lib forces inline behavior). Verify that when `mode == EmitMode::Lib`, no separate segment modules are emitted — all QRLs stay as `inlinedQrl()` in the root module.

    In lib.rs, the segment emission loop already skips inline segments (`if record.is_inline { continue; }`). For Lib mode, all segments should be marked inline. Verify this is the case by checking how `is_inline` on SegmentRecord is set.

    If Lib mode segments are NOT being marked as inline, add a check: when `mode == EmitMode::Lib`, force `is_inline = true` on all SegmentRecords.

    **Part C: Validate Test mode const-replacement skip**

    Check const_replace.rs — it should already skip when mode is Test. If so, this is just test validation. If not, add the skip.

    Do NOT add `_useHmr` to non-component$ segments. Gate strictly on `component$` marker name.
  </action>
  <verify>
    <automated>cd /Users/scottweaver/Projects/qwik-optimizer-next && cargo test --manifest-path crates/qwik-optimizer-oxc/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - _useHmr injection in component$ segment bodies when mode is Hmr
    - No _useHmr in non-component$ segments
    - Lib mode produces no separate segment files (all inline)
    - Test mode preserves isServer/isBrowser as runtime identifiers
    - All existing tests still pass
    - New emit mode tests pass
  </acceptance_criteria>
  <done>All 5 emit modes produce correct behavioral variations. HMR injection works for component$ only. Lib mode keeps all QRLs inline. Test mode skips const replacement. Full test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Emit mode integration tests covering all 5 modes</name>
  <files>crates/qwik-optimizer-oxc/tests/snapshot_tests.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/tests/snapshot_tests.rs (first 100 lines for test patterns)
    - crates/qwik-optimizer-oxc/tests/fixtures.json (search for "hmr", "lib", "test" mode entries)
  </read_first>
  <action>
    Verify that the existing snapshot test corpus includes mode-specific test cases. The spec mentions: `example_dev_mode`, `example_dev_mode_inlined`, `example_lib_mode`, `hmr`.

    Check if fixtures.json contains these examples with appropriate mode configs. If yes:
    - Run tests and verify snapshots pass
    - Update snapshots with `cargo insta review` if Task 1 changes produced correct new output

    If HMR fixture does NOT exist in fixtures.json:
    - Add an integration test using the spec's HMR example input (lines 5042-5053): component$ + $() to verify only component$ gets _useHmr injection
    - Configure with `mode: EmitMode::Hmr` and a dev_path
    - Assert segment output contains `_useHmr` for the component$ segment
    - Assert segment output does NOT contain `_useHmr` for the $() segment

    Add focused mode validation tests:
    - Lib mode: verify output has 1 module (root only, no segments), root code contains `inlinedQrl`
    - Test mode: verify output preserves `isServer` as identifier (not `true`/`false` literal)
    - Dev mode: verify QRL calls use `qrlDEV` (already covered by existing tests, just verify)
  </action>
  <verify>
    <automated>cd /Users/scottweaver/Projects/qwik-optimizer-next && cargo test --manifest-path crates/qwik-optimizer-oxc/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HMR mode snapshot/integration tests pass
    - Lib mode test confirms no separate segments
    - Test mode test confirms no const replacement
    - Dev mode test confirms qrlDEV usage
    - Prod mode test confirms standard qrl usage
    - Full test suite green with no regressions
  </acceptance_criteria>
  <done>All 5 emit modes validated with dedicated tests. HMR _useHmr injection confirmed in component$ segments only. Full test suite green.</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
cd /Users/scottweaver/Projects/qwik-optimizer-next && cargo test --manifest-path crates/qwik-optimizer-oxc/Cargo.toml
```
All tests pass including new emit mode tests. No regressions.
</verification>

<success_criteria>
- All 5 emit modes produce correct behavioral variations per the Mode x CONV table in the spec
- HMR _useHmr() injected only in component$ bodies
- Lib mode produces no separate segment files
- Test mode skips const replacement
- Dev/Hmr modes use qrlDEV/inlinedQrlDEV
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-strategies-modes-binding-implementation/06-02-SUMMARY.md`
</output>
