---
phase: 09-metadata-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - crates/qwik-optimizer-oxc/Cargo.toml
autonomous: true
requirements:
  - SPEC-06
  - SPEC-18
  - SPEC-19
  - SPEC-20
  - IMPL-03
  - IMPL-04

must_haves:
  truths:
    - "Every requirement checkbox in REQUIREMENTS.md reflects actual completion evidence"
    - "ROADMAP.md progress table has correct plan counts, completion dates, and statuses"
    - "The parallel feature flag and rayon dependency are removed from Cargo.toml"
    - "IMPL-03 and IMPL-04 checkboxes reflect actual entry strategy and emit mode support"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement completion checkboxes and traceability table"
      contains: "SPEC-06.*Complete|SPEC-18.*Complete|SPEC-19.*Complete|SPEC-20.*Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate progress table with plan counts and completion dates"
      contains: "Complete"
    - path: "crates/qwik-optimizer-oxc/Cargo.toml"
      provides: "Clean dependency list without dead parallel feature"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Requirement statuses match phase completion statuses"
      pattern: "Complete|Pending"
---

<objective>
Investigate actual completion status of IMPL-03/04 and SPEC-06/18/19/20, reconcile REQUIREMENTS.md checkboxes and traceability table, fix ROADMAP.md progress table, and remove the dead parallel feature flag.

Purpose: Close the metadata gap where requirement checkboxes and roadmap statuses are stale, and remove dead code (per D-52, D-54, D-55).
Output: Accurate REQUIREMENTS.md, accurate ROADMAP.md, clean Cargo.toml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-metadata-verification-cleanup/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate requirements and update REQUIREMENTS.md + ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current checkbox state and traceability table)
    - .planning/ROADMAP.md (current progress table and phase details)
    - specification/qwik-optimizer-spec.md (search for CONV-06 JSX Transform section, TransformModulesOptions, TransformOutput, Diagnostic sections to verify SPEC-06/18/19/20)
    - crates/qwik-optimizer-oxc/src/entry_strategy.rs (all 7 strategies implemented: Inline, Hoist, Single, Hook, Segment, Component, Smart via parse_entry_strategy match arms at lines 92-96)
    - crates/qwik-optimizer-oxc/src/types.rs (EntryStrategy enum at line 287 with all 7 variants; EmitMode enum at line 341 with Lib, Prod, Dev, Hmr, Test)
    - crates/qwik-optimizer-oxc/src/transform.rs (EmitMode usage throughout: Dev/Hmr QRL variants, Lib skip patterns, Test const replacement skip)
    - crates/qwik-optimizer-oxc/src/const_replace.rs (EmitMode::Lib and EmitMode::Test skip logic at line 41)
  </read_first>
  <action>
    Investigate and update 6 pending requirements per D-54 and D-55:

    **SPEC-06 (JSX Transform):** Search spec document for "### JSX Transform (CONV-06)" section. It exists at line 2011 with full coverage: _jsxSorted/_jsxSplit branch point, element transformation, prop classification (static/dynamic), special attributes (children, ref, q:slot, className, bind:value, bind:checked, on*$ events), event name conversion, key generation, spread props handling, iteration variable lifting. Mark as `[x]` Complete.

    **SPEC-18 (TransformModulesOptions):** Search spec for "TransformModulesOptions" type definition with all config fields, types, defaults, valid values. Mark checkbox based on evidence found.

    **SPEC-19 (TransformOutput/TransformModule/SegmentAnalysis/SegmentKind):** Search spec for these type definitions with field semantics. Mark checkbox based on evidence.

    **SPEC-20 (Diagnostic type):** Search spec for Diagnostic type and error/warning categories. Mark checkbox based on evidence.

    **IMPL-03 (Entry Strategies):** All 7 entry strategies are implemented in entry_strategy.rs lines 92-96:
    - EntryStrategy::Inline | EntryStrategy::Hoist => InlineStrategy
    - EntryStrategy::Single => SingleStrategy
    - EntryStrategy::Hook | EntryStrategy::Segment => PerSegmentStrategy
    - EntryStrategy::Component => PerComponentStrategy
    - EntryStrategy::Smart => SmartStrategy
    Tests exist for all 7 in entry_strategy.rs lines 152-325. Mark as `[x]` Complete.

    **IMPL-04 (Emit Modes):** All 5 emit modes are defined in types.rs (Lib, Prod, Dev, Hmr, Test) and used throughout:
    - Prod: short symbol names (s_{hash}) in hash.rs
    - Dev/Hmr: qrlDEV/inlinedQrlDEV variants in transform.rs
    - Lib: skip consumed import stripping, skip PURE injection in transform.rs
    - Test: skip const replacement in const_replace.rs
    Mark as `[x]` Complete.

    **Update REQUIREMENTS.md:**
    1. Change checkboxes for all 6 requirements from `[ ]` to `[x]` based on evidence above
    2. Update traceability table: change status from "Pending" to "Complete" for SPEC-06, SPEC-18, SPEC-19, SPEC-20, IMPL-03, IMPL-04
    3. Update the "Last updated" line at bottom to "2026-04-03 after Phase 9 metadata cleanup"

    **Update ROADMAP.md progress table and phase details:**
    1. Phase 1: Change "1/5" to "3/5" (01-01, 01-03, 01-05 are checked), keep "In Progress"
    2. Phase 3: Change "0/?" to "4/4" (03-01 through 03-04 summaries exist), change "Not started" to "Complete", add completion date
    3. Phase 4: Already shows "3/3 Complete" -- verify correct
    4. Phase 5: Change "6/7" to "7/7" (all 7 summaries exist), change "In Progress" to "Complete", add completion date
    5. Phase 6: Change "1/3" to "3/3" (06-01, 06-02, 06-03 summaries exist), change "In Progress" to "Complete", add completion date
    6. Phase 7: Already shows "2/2 Complete" -- verify correct
    7. Phase 8: Change "3/5" to "5/5" (08-01 through 08-05 summaries exist), change "In Progress" to "Complete", add completion date "2026-04-03"
    8. Phase 9: Update to "0/3" and "In Progress"
    9. Update phase details plan lists: mark 06-01 and 06-02 with [x], mark all Phase 3 plans with [x]
    10. Update Phase 3 Plans line from "TBD" to "4 plans"
    11. Update Phase 9 Plans line from "TBD" to "3 plans"

    Also update these ROADMAP phase detail sections:
    - Phase 2 Plans: should say "TBD" (not started, no plans created)
    - Phase 4 Plans: should say "TBD" or the actual count if plans exist
  </action>
  <verify>
    <automated>grep -c "\- \[ \]" .planning/REQUIREMENTS.md | head -1</automated>
    The count should show 0 checked-off spec/impl requirements remaining as pending (only REF-* and v2/deferred items should be unchecked). Specifically:
    - grep "SPEC-06.*Complete" .planning/REQUIREMENTS.md returns a match
    - grep "IMPL-03.*Complete" .planning/REQUIREMENTS.md returns a match
    - grep "IMPL-04.*Complete" .planning/REQUIREMENTS.md returns a match
    - grep "5/5.*Complete" .planning/ROADMAP.md returns matches for phases 5 and 8
  </verify>
  <done>
    All 6 pending requirement checkboxes updated to reflect actual completion. Traceability table statuses all say "Complete" for SPEC-06/18/19/20 and IMPL-03/04. ROADMAP progress table shows accurate plan counts, statuses, and completion dates for all 9 phases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead parallel feature flag from Cargo.toml</name>
  <files>crates/qwik-optimizer-oxc/Cargo.toml</files>
  <read_first>
    - crates/qwik-optimizer-oxc/Cargo.toml (current state with rayon optional dep and parallel feature)
  </read_first>
  <action>
    Per D-52, remove the dead parallel feature flag:

    1. Remove the rayon dependency line:
       ```
       rayon = { version = "1", optional = true }
       ```

    2. Remove the [features] section entirely (it only contains `default = []` and `parallel = ["rayon"]`):
       ```
       [features]
       default = []
       parallel = ["rayon"]
       ```

    3. Run `cargo check -p qwik-optimizer-oxc` to confirm the crate still compiles without the feature.

    PERF-02 in REQUIREMENTS.md explicitly defers parallel processing to v2 -- re-add when that work begins.
  </action>
  <verify>
    <automated>cd /Users/scottweaver/Projects/qwik-optimizer-next && grep -c "rayon\|parallel" crates/qwik-optimizer-oxc/Cargo.toml; cargo check -p qwik-optimizer-oxc 2>&1 | tail -3</automated>
    The grep count should be 0 (no rayon or parallel references). cargo check should succeed.
  </verify>
  <done>
    Cargo.toml has no rayon dependency, no parallel feature, and `cargo check` passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "\- \[x\] \*\*SPEC-06\*\*" .planning/REQUIREMENTS.md` returns 1
2. `grep -c "\- \[x\] \*\*IMPL-03\*\*" .planning/REQUIREMENTS.md` returns 1
3. `grep -c "\- \[x\] \*\*IMPL-04\*\*" .planning/REQUIREMENTS.md` returns 1
4. `grep -c "rayon" crates/qwik-optimizer-oxc/Cargo.toml` returns 0
5. `cargo check -p qwik-optimizer-oxc` succeeds
</verification>

<success_criteria>
- All 6 requirement checkboxes (SPEC-06, SPEC-18, SPEC-19, SPEC-20, IMPL-03, IMPL-04) reflect actual completion evidence
- ROADMAP progress table has correct plan counts for all 9 phases
- Parallel feature and rayon dependency removed from Cargo.toml
- `cargo check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-metadata-verification-cleanup/09-01-SUMMARY.md`
</output>
