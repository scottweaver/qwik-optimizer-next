---
phase: 03-build-modes-remaining-transforms-specification
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - specification/qwik-optimizer-spec.md
autonomous: true
requirements: [SPEC-17]

must_haves:
  truths:
    - "Spec contains a Mermaid DAG diagram showing the full 20-step transformation pipeline with dependency arrows"
    - "Spec contains a constraints table listing each ordering dependency with source rationale"
    - "An implementer can determine the correct execution order of all CONVs from the DAG alone"
  artifacts:
    - path: "specification/qwik-optimizer-spec.md"
      provides: "Transformation Pipeline section with Mermaid DAG and constraints table"
      contains: "## Transformation Pipeline"
  key_links:
    - from: "Pipeline DAG"
      to: "All CONV sections"
      via: "Step numbers cross-reference CONV pipeline positions documented in Stages 3-6"
      pattern: "Step.*CONV"
---

<objective>
Write the "Transformation Pipeline" section of the spec document, containing a Mermaid DAG diagram showing transformation dependencies and a constraints table listing each ordering dependency with rationale. This is the capstone section that ties together all CONVs from Phases 1-3.

Purpose: Per D-28, the pipeline DAG is both a visual diagram (Mermaid, consistent with Phase 1's pipeline diagram) and a structured constraints table. An implementer can verify their pipeline ordering is correct by checking against both representations.
Output: ~150-250 lines appended to qwik-optimizer-spec.md as the final ## section for Phase 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-build-modes-remaining-transforms-specification/03-CONTEXT.md
@.planning/phases/03-build-modes-remaining-transforms-specification/03-RESEARCH.md
@.planning/phases/03-build-modes-remaining-transforms-specification/03-01-SUMMARY.md
@.planning/phases/03-build-modes-remaining-transforms-specification/03-02-SUMMARY.md
@specification/qwik-optimizer-spec.md (read lines 11-90 — existing Pipeline Overview diagram for consistency)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read parse.rs pipeline and existing Pipeline Overview for DAG construction</name>
  <files>specification/qwik-optimizer-spec.md</files>
  <read_first>
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/parse.rs (lines 220-450 — full transform_code pipeline sequence)
    - /Users/scottweaver/Projects/qwik-optimizer-next/specification/.planning/research/FEATURES.md (search for "dependency" or "DAG" sections)
    - specification/qwik-optimizer-spec.md (lines 11-90 — existing Pipeline Overview with Mermaid diagram for style consistency)
  </read_first>
  <action>
    Read parse.rs pipeline sequence to verify the 20-step ordering from research. Read the existing Pipeline Overview Mermaid diagram (lines 15-70 of spec) to match its style (node naming, arrow syntax, subgraph grouping). Read FEATURES.md for the dependency DAG. Build the dependency graph: for each step, identify which prior steps it depends on and why. This forms the constraints table.
  </action>
  <verify>
    <automated>echo "Source files read — DAG dependencies mapped"</automated>
  </verify>
  <acceptance_criteria>20-step pipeline verified against parse.rs, dependency edges identified with rationale, existing Mermaid style noted for consistency</acceptance_criteria>
  <done>Pipeline ordering verified, all dependency edges cataloged with rationale</done>
</task>

<task type="auto">
  <name>Task 2: Write Transformation Pipeline spec section with Mermaid DAG and constraints table</name>
  <files>specification/qwik-optimizer-spec.md</files>
  <read_first>
    - specification/qwik-optimizer-spec.md (last 20 lines — insertion point after Emit Modes or Stage 6)
    - specification/qwik-optimizer-spec.md (lines 15-70 — Pipeline Overview Mermaid diagram for style matching)
  </read_first>
  <action>
    Append the following section to specification/qwik-optimizer-spec.md after the Emit Modes section:

    **## Transformation Pipeline**
    Intro paragraph: this section formalizes the dependency relationships between all pipeline steps. Cross-references the Pipeline Overview diagram (Stage 1) which shows the sequential flow; this section shows WHY that ordering is required.

    Per D-28:

    **### Pipeline Dependency DAG**
    Mermaid DAG diagram (```mermaid ... ```) showing:
    - All 20 pipeline steps as nodes (use same naming style as Pipeline Overview: "Step N: Description")
    - Dependency arrows showing which steps must precede which
    - Subgraph groupings for stages (matching Stage 2-6 organization)
    - Conditional steps annotated with their conditions (e.g., "if mode != Lib")
    - Style consistent with Phase 1's Pipeline Overview diagram (node shapes, arrow labels)

    Key dependency chains to highlight:
    1. strip_exports (step 2) -> all other transforms (runs first, modifies exports)
    2. GlobalCollect (step 7) -> all CONVs (provides import/export metadata)
    3. Props Destructuring (step 8) -> Capture Analysis (step 10) (changes variable references)
    4. Const Replacement (step 9) -> Simplifier DCE (step 12) (creates dead branches)
    5. QwikTransform (step 10) -> Treeshaker (step 11) (marks side effects before DCE)
    6. Segment Extraction (step 10) -> Variable Migration (step 14) (segments must exist)
    7. Variable Migration (step 14) -> Post-migration DCE (step 16) (migration creates unused imports)

    **### Ordering Constraints Table**
    Table with columns: Constraint | Before Step | After Step | Rationale | Source
    Each row is one ordering dependency with:
    - Human-readable constraint name
    - The step that must run first
    - The step that must run after
    - Why this ordering matters (what breaks if reversed)
    - SWC source reference (file:line)

    Include all dependencies from the research's verified DAG. Add the critical ones:
    - CONV-10 -> CONV-09: "Const replacement creates dead branches (if(true)/if(false)) that DCE removes"
    - CONV-04 -> CONV-03: "Props destructuring changes variable references that capture analysis reads"
    - strip_exports -> everything: "Throwing stubs must be in place before any transform reads export bodies"
    - GlobalCollect -> all CONVs: "Import/export metadata is read-only input to all transformations"

    **### Conditional Execution Summary**
    Compact table showing which steps are conditional and their guard expressions:
    | Step | Condition | Skipped When |
    Using the conditions from research: minify, mode, is_server, strategy checks.
  </action>
  <verify>
    <automated>grep -n "## Transformation Pipeline" specification/qwik-optimizer-spec.md && grep -n "### Pipeline Dependency DAG" specification/qwik-optimizer-spec.md && grep -n "### Ordering Constraints Table" specification/qwik-optimizer-spec.md && grep -c "mermaid" specification/qwik-optimizer-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - "## Transformation Pipeline" heading exists in spec
    - Mermaid DAG diagram present with all 20 steps and dependency arrows (per D-28)
    - Ordering Constraints Table with at least 10 dependency rows, each with rationale and source reference
    - Conditional Execution Summary table showing which steps are conditional
    - DAG style consistent with Phase 1's Pipeline Overview diagram
    - An implementer can determine correct execution order from DAG alone
  </acceptance_criteria>
  <done>Transformation Pipeline section appended with Mermaid DAG, constraints table, and conditional execution summary per D-28</done>
</task>

</tasks>

<verification>
- `grep -c "## Transformation Pipeline" specification/qwik-optimizer-spec.md` returns 1
- Mermaid code block present (grep "mermaid" returns at least 2 — opening and existing Pipeline Overview)
- Constraints table has at least 10 rows with Before/After/Rationale columns
- All 20 pipeline steps appear in the DAG
- Const Replacement -> DCE dependency explicitly documented with rationale
</verification>

<success_criteria>
- SPEC-17 (Pipeline Ordering): Full 20-step DAG in Mermaid format, constraints table with rationale for every ordering dependency, conditional execution summary, consistent with Phase 1 Pipeline Overview style
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-modes-remaining-transforms-specification/03-04-SUMMARY.md`
</output>
