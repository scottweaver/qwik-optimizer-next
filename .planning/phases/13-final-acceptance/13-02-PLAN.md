---
phase: 13-final-acceptance
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/src/emit.rs
autonomous: true
requirements: [ACC-01]

must_haves:
  truths:
    - "Every extracted segment has a corresponding `const q_NAME = /*#__PURE__*/ qrl(...)` declaration in the root module, even when the original call site was a bare expression (not assigned to a variable)"
    - "All QRL wrapper calls in the root module have /*#__PURE__*/ annotations: qrl(), componentQrl(), _jsxSorted(), _noopQrl()"
    - "Parity full match count reaches at least 100/201 after these structural fixes (up from 79/201 baseline)"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "QRL const declaration emission for all segments"
      contains: "const q_"
    - path: "crates/qwik-optimizer-oxc/src/lib.rs"
      provides: "PURE annotation injection for wrapper calls, const stripping bug fix"
    - path: "crates/qwik-optimizer-oxc/src/emit.rs"
      provides: "PURE annotation post-processing"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/transform.rs"
      to: "root module output"
      via: "exit_program QRL declaration section"
      pattern: "q_.*qrl"
---

<objective>
Fix QRL const declaration emission, PURE annotation placement, and const stripping bug in root module output.

Purpose: Categories 3 (missing PURE, 48 fixtures) and 4 (missing QRL const declarations, 35 fixtures) are the highest-impact structural fixes. The SWC root module always emits `const q_NAME = /*#__PURE__*/ qrl(...)` for every extracted segment, and wraps component/JSX wrapper calls with `/*#__PURE__*/`. The OXC implementation currently emits bare expressions for some QRLs and is missing PURE annotations on wrapper calls like `componentQrl()`. Additionally, the const stripping bug (11-VERIFICATION.md Gap 1) strips q_* hoisted declarations, breaking output.

Output: Root module QRL declarations always use `const q_NAME = ...` pattern; all wrapper calls have PURE annotations; const stripping excludes q_* lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-final-acceptance/13-CONTEXT.md
@.planning/phases/13-final-acceptance/13-RESEARCH.md
@.planning/phases/13-final-acceptance/13-01-SUMMARY.md

<interfaces>
From crates/qwik-optimizer-oxc/tests/swc_expected/example_1.snap (root module section):
```
import { qrl } from "@qwik.dev/core";
import { component } from '@qwik.dev/core';
//
const q_renderHeader1_jMxQsjbyDss = /*#__PURE__*/ qrl(()=>import("./test.tsx_renderHeader1_jMxQsjbyDss"), "renderHeader1_jMxQsjbyDss");
const q_renderHeader2_component_Ay6ibkfFYsw = /*#__PURE__*/ qrl(()=>import("./test.tsx_renderHeader2_component_Ay6ibkfFYsw"), "renderHeader2_component_Ay6ibkfFYsw");
//
export const renderHeader1 = q_renderHeader1_jMxQsjbyDss;
component(q_renderHeader2_component_Ay6ibkfFYsw);
```

From crates/qwik-optimizer-oxc/tests/swc_expected/example_functional_component.snap (root module):
```
import { componentQrl } from "@qwik.dev/core";
import { qrl } from "@qwik.dev/core";
import { $, component$, useStore } from '@qwik.dev/core';
//
const q_Header_component_J4uyIhaBNR4 = /*#__PURE__*/ qrl(()=>import("./test.tsx_Header_component_J4uyIhaBNR4"), "Header_component_J4uyIhaBNR4");
//
const Header = /*#__PURE__*/ componentQrl(q_Header_component_J4uyIhaBNR4);
```

Pattern: SWC emits `const q_NAME = /*#__PURE__*/ qrl(...)` for EVERY segment. Then in the body section, wrapper calls use the q_NAME variable: `/*#__PURE__*/ componentQrl(q_Header_component_J4uyIhaBNR4)`.

From crates/qwik-optimizer-oxc/src/lib.rs line 832-850:
The `strip_unreferenced_wrapper_consts_text` function at line 814 includes `const q_*` lines as stripping candidates because `is_pure = rhs.contains("__PURE__")` is true for them. These hoisted QRL declarations MUST be excluded.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix const stripping bug and emit const q_NAME declarations for all extracted segments</name>
  <files>crates/qwik-optimizer-oxc/src/lib.rs, crates/qwik-optimizer-oxc/src/transform.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/src/lib.rs (lines 814-890, strip_unreferenced_wrapper_consts_text -- the candidate filter at lines 832-850 must exclude `const q_*` lines)
    - crates/qwik-optimizer-oxc/src/transform.rs (find exit_program method -- look for where qrl() calls are generated and how they're inserted into the program body)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_1.snap (SWC reference showing const q_ pattern)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_functional_component.snap (SWC reference showing componentQrl(q_NAME) pattern)
  </read_first>
  <action>
Two changes:

**Change 1 -- Fix const stripping bug in lib.rs:**

In `strip_unreferenced_wrapper_consts_text` at the candidate filter (around line 832-850), add an exclusion for `const q_*` lines BEFORE the `is_call || is_pure || is_simple_q_ref` check. Add this guard after extracting the `name` variable:

```rust
// Never strip hoisted QRL const declarations (const q_*)
if name.starts_with("q_") {
    continue;
}
```

This goes right after the destructuring pattern skip (line 839-841), before the RHS analysis. The `const q_*` lines are hoisted QRL references that are used in the module body; stripping them creates dangling references (e.g., `component(q_renderHeader2_...)` with no matching const).

**Change 2 -- Ensure every segment gets a const q_NAME declaration in transform.rs:**

In `exit_program`, find where QRL declarations are emitted. The SWC pattern always emits `const q_NAME = /*#__PURE__*/ qrl(...)` for every extracted segment, even when the original call site was a bare expression (not assigned to a variable). Verify the OXC implementation does the same. If some QRL calls are emitted as bare expressions (without `const q_NAME =`), fix the emission to always wrap in a const declaration.

Run `cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_diff_report -- --nocapture 2>&1 | head -200` (from Plan 01) to understand the exact differences before and after changes.

IMPORTANT: After making changes, run the FULL test suite to check for regressions: `cargo test -p qwik-optimizer-oxc`
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc && cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep -E "Full match|Root module"</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p qwik-optimizer-oxc exits with status 0 (no regressions)
    - cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report shows Root module match > 80/201
    - For example_1 fixture: the root module output contains "const q_renderHeader1" and "const q_renderHeader2_component"
    - grep -c 'starts_with("q_")' lib.rs returns at least 2 (the exclusion guard)
  </acceptance_criteria>
  <done>Const stripping bug fixed -- q_* lines excluded from candidates. All extracted segments have const q_NAME declarations in root module. Root module match count increases from 80/201 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Add PURE annotations to wrapper calls in root module body</name>
  <files>crates/qwik-optimizer-oxc/src/emit.rs, crates/qwik-optimizer-oxc/src/transform.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/src/emit.rs (see normalize_pure_annotations -- already converts OXC's /* @__PURE__ */ to /*#__PURE__*/, but the issue is PURE annotations not being emitted at all for wrapper calls)
    - crates/qwik-optimizer-oxc/src/transform.rs (find where componentQrl(), _jsxSorted(), _noopQrl() calls are generated in exit_program -- these need PURE annotations)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_functional_component.snap (line 59: `const Header = /*#__PURE__*/ componentQrl(q_Header_component_J4uyIhaBNR4)`)
  </read_first>
  <action>
The SWC output adds `/*#__PURE__*/` annotations before these call expressions in the root module:
- `qrl()` / `qrlDEV()` calls in the hoisted const declarations
- `componentQrl()` calls in the body
- `_jsxSorted()` / `_jsxSplit()` calls in lightweight component bodies
- `_noopQrl()` calls (Inline strategy)

There are two approaches:
1. **AST-level**: Add `#[pure]` leading comments to the call expression AST nodes in transform.rs before codegen. OXC codegen will then emit `/* @__PURE__ */` which normalize_pure_annotations converts to `/*#__PURE__*/`.
2. **Post-processing**: After codegen, inject `/*#__PURE__*/` before specific function calls via string manipulation.

Approach 1 is preferred if OXC supports adding leading comments to expression nodes. Investigate `oxc::ast::ast::Expression` and whether it supports `leading_comments` or if `Span`-based comment injection is available.

If AST-level is not straightforward, use approach 2: add a post-processing step in emit.rs that inserts `/*#__PURE__*/` before known wrapper call patterns: `qrl(`, `qrlDEV(`, `componentQrl(`, `_jsxSorted(`, `_jsxSplit(`, `_noopQrl(`. Only insert when the annotation is not already present.

Ensure the annotation appears on the SAME LINE as the call, directly before it, separated by a space: `/*#__PURE__*/ componentQrl(...)`.

Run full test suite after changes.
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc && cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep -E "Full match|Root module"</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p qwik-optimizer-oxc exits with status 0 (no regressions)
    - cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report shows Full match >= 100/201
    - For example_functional_component: root module output contains "/*#__PURE__*/ componentQrl("
  </acceptance_criteria>
  <done>PURE annotations present on all QRL wrapper calls in root module. Full match count reaches at least 100/201. No regressions.</done>
</task>

</tasks>

<verification>
1. `cargo test -p qwik-optimizer-oxc` -- all tests pass (no regressions)
2. `cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture` -- Full match >= 100/201
3. Root module output for example_1 contains `const q_renderHeader1` and `const q_renderHeader2_component`
4. Root module output for example_functional_component contains `/*#__PURE__*/ componentQrl(`
</verification>

<success_criteria>
- QRL const declarations emitted for all segments, not just variable-assigned ones
- PURE annotations on all wrapper calls in root module
- Const stripping bug fixed (q_* lines excluded from candidates)
- Full match count reaches at least 100/201 (up from 79/201 baseline)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-acceptance/13-02-SUMMARY.md`
</output>
