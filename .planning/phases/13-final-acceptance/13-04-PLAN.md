---
phase: 13-final-acceptance
plan: 04
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - crates/qwik-optimizer-oxc/src/words.rs
  - crates/qwik-optimizer-oxc/src/hash.rs
  - crates/qwik-optimizer-oxc/src/transform.rs
autonomous: true
requirements: [ACC-01]

must_haves:
  truths:
    - "Segment hashes match SWC output for all 201 fixtures -- no counter-based `_1` disambiguation suffixes that SWC doesn't produce"
    - "QRL naming (q_NAME_HASH) matches SWC conventions exactly"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/words.rs"
      provides: "QRL naming convention matching SWC"
    - path: "crates/qwik-optimizer-oxc/src/hash.rs"
      provides: "Hash computation matching SWC"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/hash.rs"
      to: "crates/qwik-optimizer-oxc/src/words.rs"
      via: "hash used in QRL name construction"
      pattern: "hash.*qrl_name"
---

<objective>
Fix hash computation and QRL naming to match SWC output.

Purpose: Category 6 (hash/name differences, 28 fixtures). The segment hash computation produces different hashes, and the counter-based `_1` suffix (disambiguation) appears in OXC but not SWC. These cause `const q_NAME_HASH` identifiers to differ.

Output: All QRL names and hashes match SWC for all fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-final-acceptance/13-CONTEXT.md
@.planning/phases/13-final-acceptance/13-RESEARCH.md
@.planning/phases/13-final-acceptance/13-01-SUMMARY.md
@crates/qwik-optimizer-oxc/src/words.rs
@crates/qwik-optimizer-oxc/src/hash.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hash computation to match SWC</name>
  <files>crates/qwik-optimizer-oxc/src/hash.rs, crates/qwik-optimizer-oxc/src/words.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/src/hash.rs (understand how segment content hash is computed)
    - crates/qwik-optimizer-oxc/src/words.rs (understand QRL name construction, look for counter/disambiguation logic producing `_1` suffixes)
    - crates/qwik-optimizer-oxc/src/transform.rs (where hash/naming is invoked -- find segment name generation)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/words.rs (SWC reference for naming)
  </read_first>
  <action>
Use the parity_diff_report from Plan 01 to identify exact hash differences. Run:
`cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_diff_report -- --nocapture 2>&1 | grep -i "hash\|_1_\|HASH_DIFF"` to see the specific mismatches.

Then investigate:

1. **Counter suffix `_1`**: The OXC implementation adds `_1` disambiguation suffixes (e.g., `Header_component_1_uWM1kg0IGO0`) that SWC doesn't produce (it outputs `Header_component_UVBJuFYfvDo`). Find the counter logic in words.rs or transform.rs and understand why it's needed in OXC but not SWC. The counter may be an artifact of different scope/deduplication tracking.

2. **Hash value differences**: If the segment body content is identical between SWC and OXC but the hash differs, the hash algorithm inputs differ. Compare what gets hashed -- SWC hashes the segment code string; OXC may hash different content or include additional metadata.

3. **Compare with SWC reference**: Read the SWC `words.rs` at `/Users/scottweaver/Projects/qwik/packages/optimizer/core/src/words.rs` and compare naming conventions.

Fix the naming to remove spurious `_1` counters and ensure hash inputs match SWC. This may involve adjusting the `stack_ctxt` contents that feed into the name/hash computation.

Run full test suite after changes.
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc && cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep -E "Full match|Root module"</automated>
  </verify>
  <done>QRL names and hashes match SWC output. No `_1` disambiguation suffixes that SWC doesn't produce. Full match count increases. No regressions.</done>
</task>

</tasks>

<verification>
1. `cargo test -p qwik-optimizer-oxc` -- all tests pass
2. Parity report shows improvement from hash fixes
3. example_functional_component QRL name matches SWC exactly
</verification>

<success_criteria>
- Hash computation matches SWC for all fixtures
- QRL naming conventions match SWC (no spurious counter suffixes)
- Full match count increases from prior baseline
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-acceptance/13-04-SUMMARY.md`
</output>
