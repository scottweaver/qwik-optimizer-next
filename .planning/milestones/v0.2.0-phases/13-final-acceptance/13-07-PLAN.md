---
phase: 13-final-acceptance
plan: 07
type: execute
wave: 4
depends_on: [13-04, 13-05, 13-06]
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/src/code_move.rs
  - crates/qwik-optimizer-oxc/src/collector.rs
autonomous: true
requirements: [ACC-01]

must_haves:
  truths:
    - "Import conflict renaming works (e.g., user local `componentQrl` renamed to `componentQrl1`)"
    - "Dev mode lo/hi byte offsets in qrlDEV() match SWC values"
    - "Server-mode dead code elimination matches SWC (if(false){} blocks stripped)"
    - "Segment mismatch `should_preserve_non_ident_explicit_captures` (exp=1, act=0) is investigated and fixed (per D-04)"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "Import conflict renaming, dev mode span fixes"
    - path: "crates/qwik-optimizer-oxc/src/code_move.rs"
      provides: "Segment extraction fix for non-ident explicit captures"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/transform.rs"
      to: "root module output"
      via: "import conflict detection and renaming"
      pattern: "componentQrl1"
---

<objective>
Fix remaining feature gaps: import conflict renaming, dev mode offsets, dead code elimination, TS enum transpilation, jsxImportSource, and segment extraction bug.

Purpose: Categories 10 (dev lo/hi, 5 fixtures), 12 (import conflicts, 2 fixtures), 14 (dead code, 1 fixture), 15 (TS enum, 1 fixture), 16 (jsxImportSource, 1 fixture), and the D-04 segment mismatch. These are individually low-impact but collectively needed for 201/201.

Output: All remaining feature gaps closed; segment extraction bug investigated and fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-final-acceptance/13-CONTEXT.md
@.planning/phases/13-final-acceptance/13-RESEARCH.md
@.planning/phases/13-final-acceptance/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix import conflict renaming and dev mode lo/hi offsets</name>
  <files>crates/qwik-optimizer-oxc/src/transform.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/src/transform.rs (search for import conflict handling -- does it detect when a synthesized import like `componentQrl` conflicts with a user local variable?)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/transform.rs (SWC reference for import conflict renaming -- search for "conflict" or "rename")
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_qwik_conflict.snap (SWC reference showing renamed import)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_dev_mode.snap (SWC reference showing lo/hi values)
  </read_first>
  <action>
**Fix 1 -- Import conflict renaming (Category 12, 2 fixtures):**
When user code has a local variable named `componentQrl` (or any synthesized import name), SWC renames the import to `componentQrl1`. Add conflict detection in exit_program when inserting synthesized imports:
1. Before inserting an import, check if that name exists as a binding in the program body
2. If conflict, append `1` to the import name and update all references in generated code
3. This affects example_qwik_conflict and hmr fixtures

**Fix 2 -- Dev mode lo/hi offsets (Category 10, 5 fixtures):**
The `lo` and `hi` byte offsets in `qrlDEV()` calls differ between SWC and OXC. Compare how SWC computes these (likely span of the original dollar-sign function argument) vs how OXC computes them. The offsets should point to the original source positions of the extracted function body.

Check if OXC is using span positions from a different point in the AST (e.g., the full call expression span vs the arrow function body span).

Run full test suite after changes.
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc && cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep -E "Full match|Root module"</automated>
  </verify>
  <done>Import conflicts handled with renaming. Dev mode lo/hi offsets match SWC. No regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Fix dead code stripping, TS enum, jsxImportSource, and segment extraction bug (D-04)</name>
  <files>crates/qwik-optimizer-oxc/src/lib.rs, crates/qwik-optimizer-oxc/src/transform.rs, crates/qwik-optimizer-oxc/src/code_move.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_build_server.snap (SWC dead code stripping reference)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_ts_enums.snap (SWC TS enum transpilation)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_jsx_import_source.snap (SWC jsxImportSource handling)
    - crates/qwik-optimizer-oxc/tests/fixtures/should_preserve_non_ident_explicit_captures (fixture input for D-04 segment bug)
    - crates/qwik-optimizer-oxc/src/code_move.rs (segment extraction logic)
  </read_first>
  <action>
**Fix 1 -- Dead code stripping (Category 14, 1 fixture: example_build_server):**
In server mode (`is_server: true`), SWC replaces `if (typeof window !== "undefined")` blocks with `if (false) {}` and then strips the dead code. Check if OXC's server mode handling does this replacement. If not, add it as a post-processing step or in the transform.

**Fix 2 -- TS enum transpilation (Category 15, 1 fixture: example_ts_enums):**
When `transpile_ts: true`, SWC transpiles TypeScript enums to IIFE form. Since we can't use OXC's transformer feature per CLAUDE.md, implement a targeted enum transpiler for this specific case. Check if the fixture just needs the enum keyword handled differently or if full IIFE conversion is needed.

**Fix 3 -- jsxImportSource (Category 16, 1 fixture: example_jsx_import_source):**
The `/* @jsxImportSource react */` pragma should tell the optimizer to skip Qwik JSX transforms and use React's JSX runtime. Parse the pragma from the source file's leading comments and skip Qwik-specific transforms when a non-Qwik import source is specified.

**Fix 4 -- Segment extraction bug (D-04, 1 fixture: should_preserve_non_ident_explicit_captures):**
Per D-04, this segment mismatch (exp=1, act=0) must be investigated separately. Read the fixture input and understand what non-ident explicit capture pattern it tests. Then trace through code_move.rs/transform.rs to find why the segment isn't being extracted. The explicit capture syntax might be something like `$([dep1, dep2], () => ...)` with non-identifier captures (e.g., member expressions).

Run full test suite after changes.
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc && cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep -E "Full match|Root module|Segment count"</automated>
  </verify>
  <done>Dead code stripping, TS enum handling, jsxImportSource, and non-ident explicit capture segment extraction all match SWC. D-04 investigation complete and fix applied. No regressions.</done>
</task>

</tasks>

<verification>
1. `cargo test -p qwik-optimizer-oxc` -- all tests pass
2. Parity report shows improvement
3. should_preserve_non_ident_explicit_captures segment count matches SWC (exp=1, act=1)
</verification>

<success_criteria>
- All remaining feature gaps closed
- D-04 segment mismatch fixed (per CONTEXT.md decision)
- Segment count match improved (targeting 201/201)
- No regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-acceptance/13-07-SUMMARY.md`
</output>
