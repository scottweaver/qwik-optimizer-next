---
phase: 13-final-acceptance
plan: 05
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/collector.rs
autonomous: true
requirements: [ACC-01]

must_haves:
  truths:
    - "Import statements in root module match SWC for count, specifiers, and ordering"
    - "`_wrapProp()` calls generated for reactive prop access in JSX transforms"
    - "`Fragment` import emitted when JSX fragments are used"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "Correct import generation, _wrapProp emission, Fragment import"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/transform.rs"
      to: "root module output"
      via: "exit_program import assembly"
      pattern: "imports_to_add"
---

<objective>
Fix import count differences, _wrapProp references, and Fragment imports.

Purpose: Category 2 (import count, 53 fixtures), Category 7 (_wrapProp, 16 fixtures), and Category 13 (Fragment import, 4 fixtures). These are related -- all involve missing or extra imports/references in the root module. Many import count differences will be fixed as side effects of Plans 02-04, but the remaining sub-issues (_wrapProp, Fragment, import deduplication) need direct fixes.

Output: Root module imports match SWC in count, specifiers, and ordering for all affected fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-final-acceptance/13-CONTEXT.md
@.planning/phases/13-final-acceptance/13-RESEARCH.md
@.planning/phases/13-final-acceptance/13-01-SUMMARY.md
@crates/qwik-optimizer-oxc/src/transform.rs
@crates/qwik-optimizer-oxc/src/collector.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _wrapProp emission and Fragment import generation</name>
  <files>crates/qwik-optimizer-oxc/src/transform.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/src/transform.rs (search for `needs_wrap_prop_import`, `_wrapProp`, `needs_fragment_import`, `Fragment` -- understand when these flags are set)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/transform.rs (SWC reference for _wrapProp generation and Fragment import)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_props_wrapping.snap (SWC reference showing _wrapProp usage)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_derived_signals_children.snap (SWC reference showing Fragment import)
  </read_first>
  <action>
Two fixes:

**Fix 1 -- _wrapProp generation (Category 7, 16 fixtures):**
The SWC transform wraps signal-like prop accesses in `_wrapProp(props, "propName")` for signal tracking in reactive contexts. Check if the OXC implementation:
- Sets `needs_wrap_prop_import = true` when _wrapProp is needed
- Actually emits the `_wrapProp(props, "propName")` calls in the JSX transform output
- Compare with SWC reference to understand when _wrapProp is generated (typically for `props.foo` access patterns in component bodies that become QRL segments)

If the flag is set but the calls aren't emitted, fix the call emission. If the flag isn't being set, fix the detection logic.

**Fix 2 -- Fragment import (Category 13, 4 fixtures):**
JSX Fragment (`<>...</>`) requires `import { Fragment as _Fragment } from "@qwik.dev/core/jsx-runtime"`. Check if:
- `needs_fragment_import` is set when Fragment JSX is encountered
- The import uses the correct source module (`@qwik.dev/core/jsx-runtime`, not `@qwik.dev/core`)
- The alias `_Fragment` matches SWC convention

Run the parity_diff_report filtered for these fixtures to understand exact differences.

IMPORTANT: Run full test suite after changes.
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc && cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep -E "Full match|Root module"</automated>
  </verify>
  <done>_wrapProp calls generated for reactive prop access. Fragment import emitted for JSX fragments. Import counts closer to SWC for affected fixtures. No regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining import count differences and deduplication</name>
  <files>crates/qwik-optimizer-oxc/src/transform.rs, crates/qwik-optimizer-oxc/src/collector.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/src/transform.rs (exit_program import assembly section, lines ~2740-2780)
    - crates/qwik-optimizer-oxc/src/collector.rs (import collection logic)
    - Run: cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_diff_report -- --nocapture 2>&1 | grep "IMPORT_DIFF" to see remaining import differences
  </read_first>
  <action>
After Plans 02-04 and Task 1 above have fixed many import issues, diagnose remaining import count differences using parity_diff_report output.

Common remaining sub-issues:
- OXC emitting extra imports that SWC strips (user imports that should be removed after transformation)
- Import deduplication: same specifier imported twice from different synthesis paths
- Import ordering within same source module

For each remaining IMPORT_DIFF fixture, compare OXC output with SWC snapshot and identify the specific sub-issue. Apply targeted fixes.

Run full test suite after changes.
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc && cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep -E "Full match|Root module"</automated>
  </verify>
  <done>Import count differences resolved for the majority of affected fixtures. No regressions.</done>
</task>

</tasks>

<verification>
1. `cargo test -p qwik-optimizer-oxc` -- all tests pass
2. Parity report shows improvement from import fixes
</verification>

<success_criteria>
- _wrapProp calls generated correctly
- Fragment imports emitted for JSX fragments
- Import counts match SWC for majority of fixtures
- No regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-acceptance/13-05-SUMMARY.md`
</output>
