---
phase: 13-final-acceptance
plan: 11
type: execute
wave: 3
depends_on: [13-09, 13-10]
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/jsx_transform.rs
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/src/emit.rs
  - crates/qwik-optimizer-oxc/src/code_move.rs
autonomous: true
requirements: [ACC-01]
gap_closure: true

must_haves:
  truths:
    - "Parity report shows 201/201 full match (root module + segment count + diagnostics all green)"
    - "No regressions from Phase 13 baseline (all previously passing tests still pass)"
    - "All signal optimization fixtures produce matching .s() bodies, _hf*, _wrapProp, _fnSignal"
    - "All 5 segment count mismatches are resolved"
    - "should_preserve_non_ident_explicit_captures fixture produces correct segment count and root module output"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/tests/snapshot_tests.rs"
      provides: "parity_report test confirms 201/201 full match"
  key_links:
    - from: "parity_report test"
      to: "201/201 full match output"
      via: "cargo test parity_report --nocapture"
      pattern: "Full match.*201/201"
---

<objective>
Final integration sweep to reach 201/201 parity. Fix remaining mismatches after plans 13-09 (structural) and 13-10 (signal optimization) by diagnosing specific per-fixture issues.

Purpose: Close the last gaps to achieve ACC-01 (201/201 full match). This plan handles edge cases, cross-cutting issues that span both structural and signal categories, and the 5 segment count mismatches.
Output: 201/201 full match on the parity report -- milestone complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-final-acceptance/13-CONTEXT.md
@.planning/phases/13-final-acceptance/13-09-SUMMARY.md
@.planning/phases/13-final-acceptance/13-10-SUMMARY.md
@crates/qwik-optimizer-oxc/src/transform.rs
@crates/qwik-optimizer-oxc/src/lib.rs
@crates/qwik-optimizer-oxc/src/emit.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix segment count mismatches and D-04 fixture, then remaining structural stragglers</name>
  <read_first>
    crates/qwik-optimizer-oxc/src/transform.rs
    crates/qwik-optimizer-oxc/src/lib.rs
    crates/qwik-optimizer-oxc/tests/swc_expected/example_3.snap
    crates/qwik-optimizer-oxc/tests/swc_expected/example_immutable_analysis.snap
    crates/qwik-optimizer-oxc/tests/swc_expected/example_jsx_import_source.snap
    crates/qwik-optimizer-oxc/tests/swc_expected/example_parsed_inlined_qrls.snap
    crates/qwik-optimizer-oxc/tests/swc_expected/example_qwik_react_inline.snap
    crates/qwik-optimizer-oxc/tests/swc_expected/should_preserve_non_ident_explicit_captures.snap
  </read_first>
  <files>
    crates/qwik-optimizer-oxc/src/transform.rs
    crates/qwik-optimizer-oxc/src/lib.rs
    crates/qwik-optimizer-oxc/src/emit.rs
    crates/qwik-optimizer-oxc/src/code_move.rs
  </files>
  <action>
Run `cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture` to get the current state after plans 13-09 and 13-10. Then systematically address every remaining mismatch.

**Segment count mismatches (5 fixtures):**

1. **example_3** (exp=2, act=0): This fixture has zero segments extracted. Check why dollar-sign expressions are not detected. Compare the fixture input to understand what QRLs should be extracted.

2. **example_immutable_analysis** (exp=5, act=0): Five segments expected, zero extracted. This is likely a complex fixture with multiple nested dollar-sign calls. Check if the fixture uses patterns not yet handled by the segment extraction logic.

3. **example_jsx_import_source** (exp=1, act=3): Too many segments extracted. Check if the `jsxImportSource` pragma is causing extra/wrong extractions. SWC expects 1 segment but OXC produces 3.

4. **example_parsed_inlined_qrls** (exp=0, act=4): No segments expected, but 4 are extracted. These are pre-compiled inlinedQrl calls that should NOT produce new segments. Check that inlinedQrl calls with existing hashes don't trigger segment extraction.

5. **example_qwik_react_inline** (exp=0, act=2): No segments expected, 2 extracted. Similar to above -- check if inline strategy or qwik-react patterns are incorrectly extracting segments.

**D-04 fixture (per user decision D-04 -- must investigate separately):**

6. **should_preserve_non_ident_explicit_captures** (exp=1, act=0): This is a segment extraction bug where a segment should be created but is not. Per D-04 in 13-CONTEXT.md, this must be investigated separately. First check if plans 13-01 through 13-10 already resolved this (run the parity report and check this specific fixture). If it still fails: examine the fixture input to understand what dollar-sign expression should produce the segment. The issue is likely that non-identifier explicit captures (e.g., `[obj.prop]` in the capture array) cause the segment extraction to bail out. Fix the capture analysis to handle non-ident expressions.

**Remaining root module mismatches:**

After fixing segment counts and the D-04 fixture, run `cargo test parity_diff_report -- --nocapture` and examine each remaining failure. For each:

1. Read the SWC expected snapshot (`crates/qwik-optimizer-oxc/tests/swc_expected/{name}.snap`)
2. Read the OXC actual output (from the snapshot test)
3. Identify the specific diff
4. Fix the root cause

Common remaining issues to check:
- Incomplete signal body emission (edge cases from plan 13-10)
- Import ordering (alphabetical within framework imports)
- Comment separator placement
- Expression vs statement QRL emission
- _restProps destructuring for spread props
- Dead code stripping edge cases

**Fallback for partial signal optimization from 13-10:**
If plan 13-10 delivered partial signal optimization (some _fnSignal/_wrapProp fixtures still fail), diagnose the specific failure mode:
- If `create_synthetic_qqsegment` is not being called for certain JSX patterns, add the missing call sites in `jsx_transform.rs` (try_wrap_prop / convert_inlined_fn)
- If `_hf*` hoisting is correct but `.s()` body doesn't include the optimized props, check the ref_assignment body builder in `transform.rs::exit_call_expression`
- If `.w()` capture arrays are missing, check scoped_idents mapping in the segment emission path
- Reference the `create_synthetic_qqsegment` architecture from 13-10 and extend it for the missed edge cases

**Approach:** Work through fixtures alphabetically. Fix the root cause (not individual symptoms) so each fix cascades to multiple fixtures. Re-run parity_report after each batch of fixes.

**If 201/201 is not achievable in this task**, document exactly which fixtures remain and why. Create a clear list of remaining issues for a potential follow-up plan.
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep "Full match"</automated>
  </verify>
  <done>Full match count reaches 201/201 (or as close as possible). Segment count matches 201/201. All 224 snapshot tests pass. should_preserve_non_ident_explicit_captures is verified (either already fixed by prior plans or fixed here). If 201/201 is not reached, every remaining mismatch is documented with its root cause.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification and snapshot update</name>
  <read_first>
    crates/qwik-optimizer-oxc/tests/snapshot_tests.rs (parity_report function)
  </read_first>
  <files>
    crates/qwik-optimizer-oxc/src/transform.rs
    crates/qwik-optimizer-oxc/src/jsx_transform.rs
    crates/qwik-optimizer-oxc/src/emit.rs
  </files>
  <action>
1. Run `cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture` and capture the full output
2. If any fixtures still mismatch, run `cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_diff_report -- --nocapture` to get detailed diffs
3. For any remaining mismatches, attempt one more pass of fixes
4. Run `cargo insta review` if any snapshot files need updating
5. Run the full test suite: `cargo test -p qwik-optimizer-oxc` to confirm no regressions
6. Verify: `cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep "Full match"` should show `201/201`

If 201/201 is achieved:
- The ACC-01 requirement is satisfied
- The v0.2.0 milestone is complete
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep "Full match.*201/201"</automated>
  </verify>
  <done>Parity report confirms 201/201 full match. All tests pass. ACC-01 requirement is satisfied.</done>
</task>

</tasks>

<verification>
# Primary acceptance gate (per D-02):
cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture
# Must show: Full match: 201/201 (100%)

# No regressions:
cargo test -p qwik-optimizer-oxc --test snapshot_tests 2>&1 | tail -3
# Must show: 0 failed

# Detailed diff (should be empty):
cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_diff_report -- --nocapture 2>&1 | grep "Total root mismatches"
# Must show: Total root mismatches: 0
</verification>

<success_criteria>
- Parity report shows 201/201 full match (100%)
- Root module match: 201/201
- Segment count match: 201/201
- Diagnostics match: 201/201
- All snapshot tests pass with 0 failures
- should_preserve_non_ident_explicit_captures fixture passes (per D-04)
- ACC-01 requirement is satisfied
- v0.2.0 milestone is complete
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-acceptance/13-11-SUMMARY.md`
</output>
