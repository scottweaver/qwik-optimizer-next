---
phase: 13-final-acceptance
plan: 08
type: execute
wave: 5
depends_on: [13-04, 13-05, 13-06, 13-07]
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/emit.rs
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/src/code_move.rs
autonomous: true
requirements: [ACC-01]

must_haves:
  truths:
    - "Parity report shows 201/201 full match (root module + segment count + diagnostics)"
    - "All existing tests pass with no regressions"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/tests/snapshot_tests.rs"
      provides: "parity_report test showing 201/201"
  key_links:
    - from: "parity_report"
      to: "201/201 full match"
      via: "all root module, segment count, and diagnostic checks pass"
      pattern: "Full match: 201/201"
---

<objective>
Final straggler sweep: diagnose and fix any remaining mismatches to reach 201/201.

Purpose: After Plans 01-07 fix all identified categories, this plan addresses any remaining stragglers discovered during verification. Per D-08, this phase uses iterative planning -- this plan handles the tail of fixtures that don't fall into clean categories or were missed by earlier fixes.

Output: 201/201 full match achieved. ACC-01 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-final-acceptance/13-CONTEXT.md
@.planning/phases/13-final-acceptance/13-RESEARCH.md
@.planning/phases/13-final-acceptance/13-01-SUMMARY.md
@.planning/phases/13-final-acceptance/13-02-SUMMARY.md
@.planning/phases/13-final-acceptance/13-03-SUMMARY.md
@.planning/phases/13-final-acceptance/13-04-SUMMARY.md
@.planning/phases/13-final-acceptance/13-05-SUMMARY.md
@.planning/phases/13-final-acceptance/13-06-SUMMARY.md
@.planning/phases/13-final-acceptance/13-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose remaining mismatches and apply targeted fixes</name>
  <files>crates/qwik-optimizer-oxc/src/transform.rs, crates/qwik-optimizer-oxc/src/emit.rs, crates/qwik-optimizer-oxc/src/lib.rs, crates/qwik-optimizer-oxc/src/code_move.rs</files>
  <read_first>
    - Run: cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture (get current status)
    - Run: cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_diff_report -- --nocapture (get detailed diffs for remaining mismatches)
    - Read SUMMARY files from Plans 01-07 to understand what was already fixed
  </read_first>
  <action>
Run the parity_diff_report to identify ALL remaining mismatches. For each:

1. Read the SWC expected snapshot
2. Compare with OXC actual output
3. Identify the specific difference
4. Apply a targeted fix

This is a catch-all task. The specific fixes depend on what remains after Plans 01-07. Common residual issues:
- Export statement ordering differences
- Semicolon or newline formatting edge cases
- Variable declaration ordering within body section
- Edge cases in any of the previously-fixed categories

Per D-05, re-run parity report after each fix to track progress and detect regressions.
Per D-07, all existing tests must continue to pass.

Continue until `Full match: 201/201` is achieved or all remaining differences are identified as needing architectural changes (escalate to user if so).
  </action>
  <verify>
    <automated>cargo test -p qwik-optimizer-oxc && cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture 2>&1 | grep "Full match"</automated>
  </verify>
  <done>Parity report shows Full match: 201/201. ACC-01 is complete. No regressions in test suite.</done>
</task>

</tasks>

<verification>
1. `cargo test -p qwik-optimizer-oxc` -- all tests pass (no regressions)
2. `cargo test -p qwik-optimizer-oxc --test snapshot_tests parity_report -- --nocapture` -- `Full match: 201/201`
3. Milestone v0.2.0 acceptance criteria met
</verification>

<success_criteria>
- Full match: 201/201 (root module + segment count + diagnostics)
- All existing tests pass
- No regressions from any prior phase
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-acceptance/13-08-SUMMARY.md`
</output>
