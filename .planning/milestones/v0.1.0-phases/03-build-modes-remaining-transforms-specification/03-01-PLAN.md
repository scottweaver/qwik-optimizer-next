---
phase: 03-build-modes-remaining-transforms-specification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specification/qwik-optimizer-spec.md
autonomous: true
requirements: [SPEC-10, SPEC-09, SPEC-11]

must_haves:
  truths:
    - "Spec describes const replacement rules for isServer/isBrowser/isDev with import source handling and mode exceptions (Lib skip, Test skip)"
    - "Spec describes DCE three-mechanism pipeline (simplifier, treeshaker mark/clean, post-migration) with conditions table"
    - "Spec describes all three code stripping mechanisms (strip_exports, strip_ctx_name, strip_event_handlers) with throwing stub generation"
  artifacts:
    - path: "specification/qwik-optimizer-spec.md"
      provides: "Stage 5: Build Environment Transforms section with CONV-10, CONV-09, CONV-11 subsections"
      contains: "## Stage 5: Build Environment Transforms"
  key_links:
    - from: "CONV-10 (Const Replacement)"
      to: "CONV-09 (DCE)"
      via: "Const replacement creates dead branches that DCE removes"
      pattern: "const replacement.*dead branch"
---

<objective>
Write the "Stage 5: Build Environment Transforms" section of the spec document, covering Const Replacement (CONV-10), Dead Branch Elimination (CONV-09), and Code Stripping (CONV-11). These three are tightly coupled: const replacement feeds DCE, and code stripping is the other half of environment-based code removal.

Purpose: An implementer working on server/client splitting needs all three transforms documented together with their ordering dependencies and conditional execution rules.
Output: ~300-500 lines appended to qwik-optimizer-spec.md as a new ## Stage 5 section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-build-modes-remaining-transforms-specification/03-CONTEXT.md
@.planning/phases/03-build-modes-remaining-transforms-specification/03-RESEARCH.md
@specification/qwik-optimizer-spec.md (read last 50 lines for insertion point)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read SWC source files and snapshots for build environment transforms</name>
  <files>specification/qwik-optimizer-spec.md</files>
  <read_first>
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/const_replace.rs (full — 96 LOC)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/clean_side_effects.rs (full — 90 LOC)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/filter_exports.rs (full — 76 LOC)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/add_side_effect.rs (full — 67 LOC)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/parse.rs (lines 240-430 — pipeline steps 2 through 16)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/example_build_server.snap
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/example_dead_code.snap
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/example_drop_side_effects.snap
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/example_strip_server_code.snap
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/example_strip_client_code.snap
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/example_strip_exports_used.snap
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/example_strip_exports_unused.snap
    - specification/qwik-optimizer-spec.md (last 50 lines — for insertion point)
  </read_first>
  <action>
    Read all listed SWC source files and snapshots to extract exact behavioral rules. Cross-reference the research in 03-RESEARCH.md against actual source code. Note any discrepancies. Identify the exact insertion point in the spec (after the last Phase 2 section, currently Infrastructure: Source Map Generation ending around line 3817).
  </action>
  <verify>
    <automated>grep -c "const_replace\|clean_side_effects\|filter_exports\|add_side_effect" /dev/null || echo "Source files read — manual verification of behavioral extraction"</automated>
  </verify>
  <acceptance_criteria>All SWC source files and snapshots read, behavioral rules extracted, insertion point identified at end of spec document</acceptance_criteria>
  <done>All source files read, behavioral rules confirmed against research, insertion point identified</done>
</task>

<task type="auto">
  <name>Task 2: Write Stage 5 Build Environment Transforms spec section</name>
  <files>specification/qwik-optimizer-spec.md</files>
  <read_first>
    - specification/qwik-optimizer-spec.md (lines 2160-2200 — Signal Optimization section header for format reference)
    - specification/qwik-optimizer-spec.md (lines 3810-3817 — end of file for append point)
  </read_first>
  <action>
    Append the following sections to specification/qwik-optimizer-spec.md after the current last line:

    **## Stage 5: Build Environment Transforms**
    Brief intro paragraph explaining these three transforms handle environment-specific code removal.

    **### Const Replacement (CONV-10)**
    Per D-04 (rules + examples), D-05 (SWC source of truth), D-06 (SWC file references):
    - SWC Source reference: `const_replace.rs` (~96 LOC)
    - Pipeline Position: Step 9 of 20
    - Rules (8 rules from research: isServer/isBrowser/isDev replacement, aliased imports, both import sources `@qwik.dev/core/build` and `@qwik.dev/core`, Lib mode skip, Test mode skip, default is_server=true)
    - Example: use `example_build_server` snapshot showing isServer=true server build (per D-16 descriptive name with snapshot name, per D-13 show input + all output modules, per D-24 inline example + "See also" list)
    - See also: list related snapshots

    **### Dead Branch Elimination (CONV-09)**
    - SWC Source reference: `clean_side_effects.rs` (~90 LOC) + `parse.rs` simplifier calls
    - Pipeline Position: Steps 11-13c of 20
    - Three DCE mechanisms: (1) SWC Simplifier primary DCE, (2) Treeshaker two-phase mark/clean, (3) Post-migration DCE
    - Conditions table showing when each mechanism runs (minify, is_server, mode, strategy conditions)
    - Why Inline/Hoist skip Treeshaker clean (SideEffectVisitor runs instead)
    - Example: use `example_dead_code` snapshot (per D-16, D-13, D-24)
    - Second example or "See also": `example_drop_side_effects` for treeshaker

    **### Code Stripping (CONV-11)**
    - SWC Source reference: `filter_exports.rs` (~76 LOC) + `transform.rs` `should_emit_segment`
    - Pipeline Position: Step 2 (strip_exports) and Step 10 (strip_ctx_name, strip_event_handlers)
    - Three mechanisms with distinct rules:
      1. strip_exports: pre-pass step 2, throwing stub generation, matches export const/function only
      2. strip_ctx_name: during QwikTransform, ctx_name prefix matching, noop QRL replacement
      3. strip_event_handlers: boolean flag, SegmentKind::EventHandler check, noop QRL replacement
    - Example: use `example_strip_exports_used` or `example_strip_exports_unused` for strip_exports
    - Example: use `example_strip_server_code` for strip_ctx_name
    - See also: `example_strip_client_code`

    Follow established spec conventions: markdown heading hierarchy, code blocks with ```js/```rust, bold for key terms, numbered rules lists.
  </action>
  <verify>
    <automated>grep -n "## Stage 5: Build Environment Transforms" specification/qwik-optimizer-spec.md && grep -n "### Const Replacement (CONV-10)" specification/qwik-optimizer-spec.md && grep -n "### Dead Branch Elimination (CONV-09)" specification/qwik-optimizer-spec.md && grep -n "### Code Stripping (CONV-11)" specification/qwik-optimizer-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - "## Stage 5: Build Environment Transforms" heading exists in spec
    - CONV-10 subsection documents all 8 const replacement rules with import source handling and mode exceptions
    - CONV-09 subsection documents all 3 DCE mechanisms with conditions table
    - CONV-11 subsection documents all 3 stripping mechanisms with throwing stub example
    - Each subsection has SWC Source reference (D-06), Rules list (D-04), at least 1 inline example with input+output (D-13, D-24), and "See also" snapshot list (D-24)
    - Descriptive example names with Jack's snapshot name in parentheses (D-16)
  </acceptance_criteria>
  <done>Stage 5 section appended to spec with CONV-10, CONV-09, CONV-11 fully documented per all locked decisions</done>
</task>

</tasks>

<verification>
- `grep -c "## Stage 5" specification/qwik-optimizer-spec.md` returns 1
- `grep -c "### Const Replacement\|### Dead Branch Elimination\|### Code Stripping" specification/qwik-optimizer-spec.md` returns 3
- Each subsection contains "SWC Source:", "Pipeline Position:", numbered Rules, and at least one Example with input/output code blocks
- Spec line count increased by ~300-500 lines from 3817
</verification>

<success_criteria>
- SPEC-10 (Const Replacement): Rules for isServer/isBrowser/isDev replacement, both import sources, Lib/Test mode exceptions documented
- SPEC-09 (Dead Branch Elimination): Three DCE mechanisms with conditions table, treeshaker two-phase approach, Inline/Hoist exception documented
- SPEC-11 (Code Stripping): All three mechanisms (strip_exports, strip_ctx_name, strip_event_handlers) with throwing stub generation documented
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-modes-remaining-transforms-specification/03-01-SUMMARY.md`
</output>
