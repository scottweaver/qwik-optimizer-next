---
phase: 04-public-api-bindings-cross-cutting-specification
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - specification/qwik-optimizer-spec.md
autonomous: true
requirements:
  - SPEC-29

must_haves:
  truths:
    - "A reader can find at least 20 representative input/output examples covering all 14 CONVs"
    - "Each example shows the snapshot name, input source code, expected output code, and which CONVs it demonstrates"
    - "Examples complement (not duplicate) the inline examples already in Phases 1-3 CONV sections"
  artifacts:
    - path: "specification/qwik-optimizer-spec.md"
      provides: "Appendix B: Representative Examples with 24 curated snapshots"
      contains: "## Appendix B: Representative Examples"
  key_links:
    - from: "## Appendix B: Representative Examples"
      to: "Jack's swc-snapshots directory"
      via: "snapshot name references"
      pattern: "example_"
---

<objective>
Write "## Appendix B: Representative Examples" appendix with 24 curated input/output examples covering all 14 CONVs, extracted from Jack's 162+ snapshot files. Per D-30, this complements inline examples already in each CONV section from Phases 1-3.

Purpose: These examples serve as verification anchors -- an implementer can check their output against these concrete input/output pairs to validate correctness across the full transformation surface.

Output: Appendix B appended as the final section of qwik-optimizer-spec.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-api-bindings-cross-cutting-specification/04-CONTEXT.md
@.planning/phases/04-public-api-bindings-cross-cutting-specification/04-RESEARCH.md
@.planning/phases/04-public-api-bindings-cross-cutting-specification/04-02-SUMMARY.md

Snapshot directories:
- /Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/ (SWC reference output)
- /Users/scottweaver/Projects/qwik-oxc-optimizer/.planning/spec/ (Jack's spec files with input source)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and write examples 1-12 (core CONVs)</name>
  <files>specification/qwik-optimizer-spec.md</files>
  <read_first>
    - specification/qwik-optimizer-spec.md (last 30 lines to find insertion point after Appendix A)
    - Research 04-RESEARCH.md curated snapshot table (lines 340-387) for the 24 selected snapshots
  </read_first>
  <action>
Append "## Appendix B: Representative Examples" section after Appendix A.

Start with an introductory paragraph explaining:
- These 24 examples are curated from Jack's 162+ SWC reference snapshots
- Each shows input source, transform configuration (entry strategy, mode), and expected output
- They cover all 14 CONVs and complement the inline examples in each CONV section
- Snapshot names correspond to files in the swc-snapshots/ test corpus

Write examples 1-12 from the research curated list. For each example:

### Example N: {Descriptive Title} (`{snapshot_name}`)

**Demonstrates:** CONV-XX ({name}), CONV-YY ({name})
**Config:** `{ entryStrategy: { type: "..." }, mode: "..." }`

**Input:**
```tsx
{source code from the spec file}
```

**Expected Output (root module):**
```js
{transformed root module from swc-snapshots}
```

**Expected Output (segments):**
```js
// {segment_filename}
{segment code}
```

**Key observations:**
- Bullet points calling out the specific transformation behaviors visible in this example
- Cross-reference to the relevant CONV section in the spec (e.g., "See CONV-03: Capture Analysis for the capture taxonomy")

For each snapshot, read the corresponding files:
1. Find the input source in `/Users/scottweaver/Projects/qwik-oxc-optimizer/.planning/spec/{snapshot_name}.md` or deduce from the snapshot
2. Find the expected output in `/Users/scottweaver/Projects/qwik-oxc-optimizer/swc-snapshots/`

The 12 examples for this task (core CONVs 01-12):
1. `example_1` — Basic dollar detection + QRL wrapping + segment extraction
2. `example_functional_component` — component$ with useStore, capture analysis
3. `example_capture_imports` — Import captures vs self-imports
4. `example_multi_capture` — Multiple captured variables
5. `destructure_args_colon_props` — Props destructuring
6. `example_segment_variable_migration` — Variable migration into segments
7. `example_jsx` — JSX _jsxSorted/_jsxSplit basics
8. `example_jsx_listeners` — Event handler JSX transforms
9. `example_derived_signals_cmp` — Signal optimization
10. `example_functional_component_2` — PURE annotation on componentQrl
11. `example_dead_code` — Dead branch elimination
12. `example_build_server` — isServer/isBrowser const replacement

IMPORTANT: Keep examples concise. If a snapshot's input/output is very long (>50 lines), extract the most relevant portion and note "[truncated for brevity -- full output in swc-snapshots/{name}]". The goal is readability, not completeness of every line.
  </action>
  <verify>
    <automated>grep -c "^### Example" specification/qwik-optimizer-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - Appendix B heading exists after Appendix A
    - 12 examples written with input, output, config, and key observations
    - CONVs 01 through 10 (at minimum) each represented by at least one example
    - Examples are concise (truncated if needed)
  </acceptance_criteria>
  <done>Appendix B started with 12 curated examples covering CONVs 01-12</done>
</task>

<task type="auto">
  <name>Task 2: Write examples 13-24 and CONV coverage table</name>
  <files>specification/qwik-optimizer-spec.md</files>
  <read_first>
    - specification/qwik-optimizer-spec.md (last 30 lines to find insertion point after example 12)
    - Research 04-RESEARCH.md curated snapshot table for examples 13-24
  </read_first>
  <action>
Continue Appendix B by writing examples 13-24 and a coverage verification table.

The 12 examples for this task (remaining CONVs + modes/strategies):
13. `example_strip_client_code` — Code stripping (strip_exports)
14. `example_strip_server_code` — Server code stripping
15. `rename_builder_io` — Legacy import rewriting
16. `example_of_synchronous_qrl` — sync$ serialization (_qrlSync)
17. `example_noop_dev_mode` — Noop QRL handling (_noopQrlDEV)
18. `example_inlined_entry_strategy` — Inline entry strategy (inlinedQrl)
19. `example_dev_mode` — Dev mode (qrlDEV variants)
20. `example_prod_node` — Prod mode (short s_ names)
21. `example_input_bind` — bind:value/bind:checked sugar
22. `should_transform_nested_loops` — Loop capture edge case
23. `example_lib_mode` — Lib mode (no const replacement)
24. `example_preserve_filenames` — preserve_filenames config effect

Same format as Task 1 for each example.

After all 24 examples, add a coverage verification table:

### CONV Coverage Summary

| CONV | Examples | Coverage |
|------|----------|----------|
| CONV-01 (Dollar Detection) | #1, #2 | Basic + component |
| CONV-02 (QRL Wrapping) | #1, #2, #18, #19, #20 | Basic + inline + dev + prod |
| CONV-03 (Capture Analysis) | #2, #3, #4, #22 | Basic + imports + multi + loops |
| ... (all 14 CONVs) | ... | ... |

End with a closing paragraph noting: "These 24 examples, combined with the inline examples in each CONV section, provide comprehensive coverage for implementation verification. The full 162+ snapshot corpus in swc-snapshots/ serves as the exhaustive behavioral test suite."
  </action>
  <verify>
    <automated>grep -c "^### Example" specification/qwik-optimizer-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - Total of 24 examples in Appendix B
    - CONV coverage table shows all 14 CONVs with at least one example each
    - Examples 13-17 cover CONVs 11-14 (the remaining CONVs)
    - Examples 18-24 show mode/strategy/config variations
    - Closing paragraph references the full 162+ snapshot corpus
  </acceptance_criteria>
  <done>All 24 examples written, CONV coverage table complete, Appendix B finished. The spec document is now complete with all Phase 4 content.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "^### Example" specification/qwik-optimizer-spec.md` returns 24
2. `grep "## Appendix B: Representative Examples" specification/qwik-optimizer-spec.md` finds the section
3. `grep "CONV Coverage Summary" specification/qwik-optimizer-spec.md` finds the coverage table
4. All 14 CONVs appear in the coverage table
5. Final section ordering: Public API Types -> Binding Contracts -> Appendix A -> Appendix B
</verification>

<success_criteria>
- SPEC-29 satisfied: 24 representative input/output examples covering all 14 CONVs
- Examples extracted from Jack's actual snapshot files (not invented)
- Each example includes snapshot name, input, output, config, and key observations per D-13/D-16
- CONV coverage table verifies all 14 CONVs represented
- Examples complement (not duplicate) inline examples from Phases 1-3 per D-30
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-api-bindings-cross-cutting-specification/04-03-SUMMARY.md`
</output>
