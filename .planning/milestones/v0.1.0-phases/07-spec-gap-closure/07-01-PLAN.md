---
phase: 07-spec-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specification/qwik-optimizer-spec.md
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-09
  - SPEC-10
  - SPEC-11

must_haves:
  truths:
    - "CONV-10 example_build_server code block is complete (has closing JSX, closing code fence, and key observations)"
    - "CONV-01 section covers all topics required by D-03 (6 rules + 3 examples + SWC references)"
    - "CONV-02 section covers all topics required by D-04 (3 QRL paths + dev variants + captures + PURE)"
    - "CONV-09 section describes 3 DCE mechanisms with conditions table and source references"
    - "CONV-11 section documents all three mechanisms including strip_event_handlers behavioral rules"
    - "Phase Coverage paragraph at spec line 89 accurately reflects that CONV-01/02 are specified in Phase 7, not Phase 1"
    - "Dangling cross-reference at spec line ~938 resolves correctly to the existing CONV-02 section"
  artifacts:
    - path: "specification/qwik-optimizer-spec.md"
      provides: "Repaired CONV-10 example and verified CONV sections"
      contains: "example_build_server"
  key_links:
    - from: "CONV-10 example block"
      to: "Stage 6: QRL Special Cases"
      via: "Properly closed code fence + key observations before Stage 6 header"
      pattern: "Key observations.*example_build_server"
---

<objective>
Verify the completeness of all CONV sections addressed by Phase 7 requirements (SPEC-01, SPEC-02, SPEC-09, SPEC-10, SPEC-11), fix the confirmed CONV-10 example truncation, and patch any other gaps found during verification.

Purpose: The research phase confirmed that all 5 CONV sections exist and are substantively complete, but identified one confirmed defect (CONV-10 `example_build_server` is truncated — missing closing JSX, code fence, and key observations) and three items requiring human verification (CONV-11 strip_event_handlers completeness, Phase Coverage accuracy, dangling cross-reference at ~line 938). This plan fixes the confirmed defect and verifies the three open questions.

Output: A repaired `specification/qwik-optimizer-spec.md` where the CONV-10 example is complete and all 5 CONV sections satisfy their requirement criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-spec-gap-closure/07-CONTEXT.md
@.planning/phases/07-spec-gap-closure/07-RESEARCH.md

<interfaces>
<!-- Key spec document locations — verified by direct inspection -->

CONV-10 truncation (lines 3882-3886 in spec):
The JSX return block is missing its closing tags. The input example ends mid-stream at:
  ```
    <Cmp>
      {isServer2 && <p>server</p>}
      {isb && <p>server</p>}
    </Cmp>
  ## Stage 6: QRL Special Cases
  ```
It should be:
  ```
    <Cmp>
      {isServer2 && <p>server</p>}
      {isb && <p>server</p>}
    </Cmp>
  );
});
```
[key observations]
[see-also cross-reference]

---

The SWC fixture for `example_build_server` (crates/qwik-optimizer-oxc/tests/swc_expected/example_build_server.snap) shows the full expected input — the JSX closes with `</Cmp>` then `);` then `});` on separate lines.

Phase Coverage paragraph is at spec line 87-91. Currently reads:
  "Phase 1 (this document) specifies: Stage 2 (GlobalCollect), Stage 4 Core Transform (Dollar Detection, Capture Analysis, QRL Wrapping, Segment Extraction, Import Rewriting)..."
This is inaccurate — Dollar Detection (CONV-01) and QRL Wrapping (CONV-02) were added in Phase 7 (gap closure), not specified in Phase 1.

Dangling cross-reference at spec line ~937-940: This is inside the Capture Analysis section's Example 1 header comment. The RESEARCH.md note was that a cross-reference "See QRL Wrapping section for create_qrl details" might be a dangling forward reference from before CONV-02 existed. Read lines 935-945 to determine current state.

CONV-11 strip_event_handlers: Research says it IS documented at line 4125. Mechanism 3 exists with behavioral rules. Verification is confirming the rules are complete (not just a stub).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CONV-10 truncated example_build_server</name>
  <files>specification/qwik-optimizer-spec.md</files>

  <read_first>
  1. Read spec lines 3852-3890 to see the exact truncation point and confirm the surrounding context.
  2. Read crates/qwik-optimizer-oxc/tests/swc_expected/example_build_server.snap to get the complete input (lines 1-92) — this is the source of truth for the input block content.
  3. Read spec lines 3979-4010 to understand the CONV-09 section that follows, to confirm the Stage 6 header belongs there (it does — the truncation cut off the end of CONV-10's example and jumped directly into Stage 6).
  </read_first>

  <action>
  The CONV-10 `example_build_server` input block is truncated. The spec currently shows:

  ```
    <Cmp>
      {isServer2 && <p>server</p>}
      {isb && <p>server</p>}
    </Cmp>
  ## Stage 6: QRL Special Cases
  ```

  The missing content (between the `</Cmp>` line and the `## Stage 6` header) is:
  ```
    );
  });
  ```
  ` ``` `

  **Key Observations:**
  - `isServer` (from `@qwik.dev/core/build`) and `isServer2` (aliased from `@qwik.dev/core`) are both `true` on a server build — the `{isServer2 && <p>server</p>}` JSX expression evaluates to `<p>server</p>` in the segment output.
  - `isBrowser` (aliased as `isb`) becomes `false` — the `if (isb) {...}` block in `functionThatNeedsWindow` is removed entirely, leaving an empty arrow function `() => {}`.
  - The `threejs` and `leaflet` imports are removed from the root module because they were only referenced in dead branches. The `mongodb` import is preserved in the segment module because it is still used after const replacement.
  - `isDev` is imported but not used as a build constant in this snippet — it passes through to the segment as a captured import.
  - The root module result demonstrates combined CONV-10 + CONV-09 + CONV-01 + CONV-02 operation.

  **See also:** `example_dead_code` (DCE removing unused imports after branch elimination), Appendix B Example 12 (isServer/isBrowser Const Replacement — full annotated walkthrough).

  ---

  Use the Edit tool to locate the exact truncation point. The edit target string is:
  ```
      {isb && <p>server</p>}
    </Cmp>
  ## Stage 6: QRL Special Cases
  ```

  Replace it with:
  ```
      {isb && <p>server</p>}
    </Cmp>
    );
  });
  ```
  ` ``` `

  **Key Observations:**
  - `isServer` (from `@qwik.dev/core/build`) and `isServer2` (aliased from `@qwik.dev/core`) are both `true` on a server build — the `{isServer2 && <p>server</p>}` JSX expression evaluates to `<p>server</p>` in the segment output.
  - `isBrowser` (aliased as `isb`) becomes `false` — the `if (isb) {...}` block in `functionThatNeedsWindow` is removed entirely, leaving an empty arrow function `() => {}`.
  - The `threejs` and `leaflet` imports are removed from the root module because they were only referenced in dead branches. The `mongodb` import is preserved in the segment module because it is still used after const replacement.
  - `isDev` is imported but not used as a build constant in this snippet — it passes through to the segment as a captured import.

  **See also:** `example_dead_code` (DCE removing unused imports after branch elimination), Appendix B Example 12 (isServer/isBrowser Const Replacement — full annotated walkthrough).

  ---

  ## Stage 6: QRL Special Cases
  ```

  Note: The Output section for example_build_server is already in Appendix B Example 12 (lines ~7214-7282 in the spec). The CONV-10 section only needs the Input block to be complete — the output walkthrough lives in Appendix B.
  </action>

  <verify>
    <automated>grep -n "isb && " /Users/scottweaver/Projects/qwik-optimizer-next/specification/qwik-optimizer-spec.md | grep -v "^7[0-9][0-9][0-9]:" | head -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "Key Observations:" specification/qwik-optimizer-spec.md` returns at least 1 result in the lines 3852-3978 range (confirming key observations section was added before Stage 6 header)
  - `grep -n "^## Stage 6" specification/qwik-optimizer-spec.md` shows Stage 6 at a line number AFTER a line containing `` ``` `` (closing fence) in the CONV-10 section
  - `grep -n "^\`\`\`$" specification/qwik-optimizer-spec.md` shows a closing fence between lines 3882 and 3887 (i.e., before Stage 6)
  - The text `</Cmp>` followed by `    );` followed by `});` followed by `` ``` `` appears as consecutive lines in the file before the Stage 6 header
  </acceptance_criteria>

  <done>CONV-10 example_build_server input block is properly closed with `);`, `});`, closing code fence, and key observations section before the `## Stage 6` heading.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CONV-01/02/09/11 and fix cross-references</name>
  <files>specification/qwik-optimizer-spec.md</files>

  <read_first>
  1. Read spec lines 428-620 (CONV-01 section) — verify all 6 D-03 required topics are present: imported marker detection, local marker detection, marker_functions HashMap, convert_qrl_word callee conversion, special cases (sync$, component$, bare $), non-marker exclusion rule.
  2. Read spec lines 619-800 (CONV-02 section) — verify all D-04 required topics are present: create_qrl path, create_inline_qrl path, create_noop_qrl path, dev mode variants, captures emission, PURE annotation rule.
  3. Read spec lines 85-93 (Phase Coverage paragraph) — check if it still claims Dollar Detection / QRL Wrapping were specified in Phase 1.
  4. Read spec lines 930-950 (around the cross-reference to CONV-02) — determine if the reference at ~line 938 is still a dangling forward reference or now resolves correctly.
  5. Read spec lines 4125-4140 (CONV-11 Mechanism 3: strip_event_handlers) — verify rules are substantive (not a stub), covering: SegmentKind check, behavioral effect, and source reference.
  </read_first>

  <action>
  Perform the following verification and patch steps:

  **Step A: Phase Coverage paragraph**
  Read lines 87-91. If the paragraph claims "Dollar Detection" and/or "QRL Wrapping" are specified in Phase 1, update it to say they are specified in Phase 7 (gap closure). The corrected text should read approximately:

  "Phase 1 (this document) specifies: Stage 2 (GlobalCollect), Stage 4 Core Transform (Capture Analysis, Segment Extraction, Import Rewriting), Stage 5 Variable Migration, and the infrastructure sections (Hash Generation, Path Resolution, Source Map Generation)."

  And in the "Later phases" sentence, add: "Phase 7 (gap closure) -- Dollar Detection (CONV-01) and QRL Wrapping (CONV-02)."

  Only make this change if the current text is inaccurate. If it was already corrected, leave it as-is.

  **Step B: Dangling cross-reference check**
  Read spec lines 930-950. If you find text that says something like "See QRL Wrapping section for create_qrl details" and it's a forward reference to a section that did not yet exist when it was written, verify that the CONV-02 section heading "### QRL Wrapping (CONV-02)" now exists (it does, at line 619). If the reference uses an anchor link, verify the anchor target exists. If the text reads as a completed cross-reference (i.e., it says "See CONV-02: QRL Wrapping" which now resolves), no change needed.

  **Step C: CONV-01 gap check**
  Read the CONV-01 section (lines 428-618). Verify each D-03 topic is present:
  - Rule 1 (imported markers from `@qwik.dev/core`)
  - Rule 2 (local marker detection, exported `$`-suffixed functions)
  - Rule 3 (`convert_qrl_word` callee conversion table)
  - Rule 4 (special cases: sync$, component$, bare $)
  - Rule 5 (detection site: fold_call_expr / QwikTransform enter_call_expression)
  - Rule 6 (non-marker exclusion)
  - 3 input/output examples
  - SWC source references (transform.rs line numbers)
  If any required topic is missing, add it following the established pattern (rule heading + description + source reference). Research indicates all topics ARE present — this is a confirm-only step unless gaps are found.

  **Step D: CONV-02 gap check**
  Read the CONV-02 section (lines 619-~800). Verify each D-04 topic is present:
  - `create_qrl` for Segment/Hook/Single/Component/Smart strategies
  - `create_inline_qrl` for Inline/Hoist strategies
  - `create_noop_qrl` for stripped callbacks
  - Dev mode variants (`qrlDEV`/`inlinedQrlDEV`)
  - Captures emission (`scoped_idents` array / emit_captures)
  - PURE annotation rule
  If any required topic is missing, add it. Research indicates all topics ARE present — confirm-only unless gaps found.

  **Step E: CONV-11 strip_event_handlers completeness check**
  Read spec lines 4125-4140 (Mechanism 3). Verify the section contains:
  - The boolean flag purpose (suppress EventHandler segments on client)
  - SegmentKind::EventHandler check rule
  - Result: noop QRL replacement (not segment extraction)
  - Source reference (transform.rs should_emit_segment)
  If the section is substantive (more than 3 lines), research is confirmed — no change needed. If it's a stub or placeholder, flesh it out based on the noop QRL Rule 1 table at line 4366 which already documents the strip_event_handlers trigger.
  </action>

  <verify>
    <automated>grep -n "Phase 7\|gap closure\|Dollar Detection\|QRL Wrapping" /Users/scottweaver/Projects/qwik-optimizer-next/specification/qwik-optimizer-spec.md | grep "^8[789]\|^9[012]:" | head -10</automated>
  </verify>

  <acceptance_criteria>
  - Phase Coverage paragraph (spec lines 87-91) either (a) no longer claims Dollar Detection/QRL Wrapping were specified in Phase 1, OR (b) was already accurate and was not changed (document which)
  - Cross-reference at spec line ~938 either (a) resolves correctly to the existing CONV-02 section, OR (b) has been updated to be a valid reference (document which)
  - `grep -c "Rule [1-6]:" specification/qwik-optimizer-spec.md` returns at least 6 results in the CONV-01 section range (lines 428-618)
  - `grep -n "create_noop_qrl\|create_inline_qrl\|create_qrl" specification/qwik-optimizer-spec.md | grep "^[6-7][0-9][0-9]:"` shows all three QRL creation paths documented in the CONV-02 section (lines 619-800)
  - `grep -n "strip_event_handlers" specification/qwik-optimizer-spec.md | grep "^412[5-9]\|^413[0-9]:"` shows at least 2 lines of substantive content in Mechanism 3 (not just a heading)
  </acceptance_criteria>

  <done>All five CONV sections (01, 02, 09, 10, 11) are verified against their requirement criteria. Phase Coverage paragraph is accurate. Dangling cross-reference is resolved. Any gaps found are patched.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -n "^## Stage 6" specification/qwik-optimizer-spec.md` — the Stage 6 header should appear AFTER a line containing `Key Observations` and a closing ` ``` ` fence in the CONV-10 example area
2. `grep -c "\- \[ \] \*\*SPEC-0[129]\|SPEC-1[01]" specification/qwik-optimizer-spec.md` — should return 0 (no broken references in the spec itself)
3. `wc -l specification/qwik-optimizer-spec.md` — line count should be > 8091 (content was added, not just moved)
</verification>

<success_criteria>
- CONV-10 `example_build_server` code block is complete: JSX closing tags, closing `});`, closing code fence, and key observations paragraph all present before `## Stage 6`
- CONV-01 section contains all 6 rules + 3 examples + SWC source references (satisfies D-03)
- CONV-02 section contains all 3 QRL creation paths + dev variants + captures + PURE rule (satisfies D-04)
- CONV-09 section has 3 DCE mechanisms + conditions table (confirmed by research, verified here)
- CONV-11 section documents all 3 mechanisms including strip_event_handlers behavioral rules
- Phase Coverage paragraph accurately describes which phase specifies each CONV
- No dangling forward references remain
</success_criteria>

<output>
After completion, create `.planning/phases/07-spec-gap-closure/07-01-SUMMARY.md` documenting:
- What was fixed (CONV-10 truncation)
- What was verified (CONV-01, CONV-02, CONV-09, CONV-11 completeness)
- What was patched (if anything in Phase Coverage or cross-references)
- Exact line numbers where changes were made
</output>
