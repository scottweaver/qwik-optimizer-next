---
phase: 08-implementation-gap-closure
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - crates/qwik-optimizer-oxc/src/jsx_transform.rs
  - crates/qwik-optimizer-oxc/src/transform.rs
autonomous: true
requirements: [IMPL-02]

must_haves:
  truths:
    - "convert_inlined_fn is called from the JSX prop classification path and _fnSignal() is emitted for applicable fixtures"
    - "_fnSignal import is emitted when signal optimization produces output"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/jsx_transform.rs"
      provides: "Signal optimization integration calling convert_inlined_fn"
      contains: "convert_inlined_fn"
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "needs_fn_signal_import flag set when signal optimization fires"
      contains: "needs_fn_signal_import = true"
  key_links:
    - from: "jsx_transform.rs prop classification"
      to: "inlined_fn.rs convert_inlined_fn"
      via: "function call during prop analysis"
      pattern: "convert_inlined_fn"
    - from: "jsx_transform.rs"
      to: "transform.rs needs_fn_signal_import"
      via: "flag propagation"
      pattern: "needs_fn_signal_import"
---

<objective>
Wire signal optimization (CONV-07) into the JSX prop classification path per D-42/D-43.

Purpose: `convert_inlined_fn` in `inlined_fn.rs` is complete but never called from the pipeline. Wiring it into JSX prop analysis completes CONV-07 support, which affects ~10 fixtures and is required for IMPL-02 (all 14 CONVs supported).

Output: Modified jsx_transform.rs with convert_inlined_fn integration, transform.rs with flag propagation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-implementation-gap-closure/08-CONTEXT.md
@.planning/phases/08-implementation-gap-closure/08-RESEARCH.md
@.planning/phases/08-implementation-gap-closure/08-01-SUMMARY.md

<interfaces>
<!-- From inlined_fn.rs: convert_inlined_fn signature -->
```rust
pub fn convert_inlined_fn<'a>(
    expr: &Expression<'a>,
    scoped_idents: &[String],
    is_const: bool,
    is_server: bool,
    allocator: &'a Allocator,
) -> (Option<String>, Option<String>, bool);
// Returns: (fn_signal_code, arrow_code, is_const_result)
// fn_signal_code is Some("_fnSignal(p0 => p0.value, [obj], 'obj.value')") when applicable
```

<!-- From jsx_transform.rs: classify_props function context -->
```rust
// classify_props is called from the JSX transform path
// It determines static vs dynamic props
// Integration point: when a prop value is dynamic, attempt signal wrapping
```

<!-- From transform.rs: QwikTransform fields needed -->
```rust
pub(crate) needs_fn_signal_import: bool,  // line 426
pub(crate) is_server: bool,               // line 438
pub(crate) scoped_idents: Vec<String>,     // from capture analysis
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire convert_inlined_fn into JSX prop classification</name>
  <files>crates/qwik-optimizer-oxc/src/jsx_transform.rs, crates/qwik-optimizer-oxc/src/transform.rs</files>
  <read_first>
    - crates/qwik-optimizer-oxc/src/inlined_fn.rs (full file -- understand convert_inlined_fn signature, eligibility checks, return values)
    - crates/qwik-optimizer-oxc/src/jsx_transform.rs (full file -- find prop classification path, understand how dynamic props are handled)
    - crates/qwik-optimizer-oxc/src/transform.rs (lines 400-440 for QwikTransform fields, lines 1587-1650 for exit_program _fnSignal import)
    - /Users/scottweaver/Projects/qwik/packages/optimizer/core/src/transform.rs (SWC signal optimization call site -- search for "convert_inlined_fn" or "_fnSignal" to see where SWC calls it)
    - /Users/scottweaver/Projects/qwik-oxc-optimizer/crates/qwik-optimizer-oxc/src/ (Jack's implementation -- search for signal optimization integration point)
    - crates/qwik-optimizer-oxc/tests/swc_expected/example_3.snap (fixture with signal optimization if applicable)
  </read_first>
  <action>
    Per D-42: `convert_inlined_fn` must be called from the JSX prop classification path in `jsx_transform.rs`. Per D-43: when it produces a signal, set `needs_fn_signal_import = true`.

    **Challenge identified in research:** `classify_props` in `jsx_transform.rs` doesn't currently have access to `scoped_idents` or `is_server` from the transform state. Two approaches:

    **Approach A (preferred):** Extend the JSX transform function signatures to accept the needed context. Pass `scoped_idents: &[String]`, `is_server: bool`, and a mutable reference to `needs_fn_signal_import: &mut bool` through the call chain from `transform.rs::exit_expression` (where JSX transform is invoked) down to `classify_props`.

    **Approach B:** Perform signal analysis at the `exit_expression` level in `transform.rs` where the transform state is available, post-processing the JSX output to replace eligible prop values with `_fnSignal()` calls.

    Use Approach A since it matches D-42's intent (integration in the prop classification path).

    Steps:
    1. Add parameters to `classify_props` (or whatever function classifies prop values as static/dynamic): `scoped_idents: &[String]`, `is_server: bool`, `allocator: &'a Allocator`
    2. For each dynamic prop value expression, call `convert_inlined_fn(expr, scoped_idents, is_const, is_server, allocator)`
    3. If `convert_inlined_fn` returns `Some(fn_signal_code)` as the first tuple element, use the signal code as the prop value instead of the original expression
    4. Return a flag indicating whether `_fnSignal` was used, so the caller in `transform.rs` can set `self.needs_fn_signal_import = true`
    5. Update the call site in `transform.rs` to pass the required context and handle the flag

    Ensure the `_fnSignal` import (already wired at transform.rs line 1631-1632) fires when the flag is set.

    Run tests after implementation to verify no regressions and check if any SWC parity fixtures now match due to signal optimization.
  </action>
  <verify>
    <automated>cd /Users/scottweaver/Projects/qwik-optimizer-next && cargo test -p qwik-optimizer-oxc 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - jsx_transform.rs contains `use crate::inlined_fn::convert_inlined_fn` or equivalent import
    - jsx_transform.rs contains a call to `convert_inlined_fn(` in the prop classification path
    - transform.rs sets `self.needs_fn_signal_import = true` when signal optimization produces output
    - `cargo test -p qwik-optimizer-oxc` exits 0
  </acceptance_criteria>
  <done>
    `convert_inlined_fn` is called from the JSX prop classification path. When a prop value expression qualifies for signal wrapping, `_fnSignal()` code is produced and the `_fnSignal` import is added to the root module. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p qwik-optimizer-oxc` -- all tests pass
- `cargo test -p qwik-optimizer-oxc --test snapshot_tests swc_parity -- --nocapture` -- parity maintained or improved from Plan 01 baseline
- grep for `convert_inlined_fn` in jsx_transform.rs confirms wiring
</verification>

<success_criteria>
- CONV-07 signal optimization is wired into the pipeline (convert_inlined_fn called from prop classification)
- _fnSignal import emitted when needed
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-implementation-gap-closure/08-02-SUMMARY.md`
</output>
